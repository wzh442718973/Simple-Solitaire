 

package com.ardently.love.delightgame;
import java.util.Random;


import java.util.HashMap;



import android.content.res.Resources;
import androidx.appcompat.app.AppCompatActivity;
import android.util.Log;

import java.util.ArrayList;

import com.ardently.love.delightgame.games.NBillingAnimate;
import com.ardently.love.delightgame.games.VInfoCheck;
import com.ardently.love.delightgame.games.YEditPeaks;
import com.ardently.love.delightgame.games.IProxy;
import com.ardently.love.delightgame.games.LVBaseResizingCell;
import com.ardently.love.delightgame.games.TAnimation;
import com.ardently.love.delightgame.games.PJCPurpleLeft;
import com.ardently.love.delightgame.games.NAWait;
import com.ardently.love.delightgame.games.GSColors;
import com.ardently.love.delightgame.games.WIEAutofill;
import com.ardently.love.delightgame.games.JWEDarkLoad;
import com.ardently.love.delightgame.games.EUYEncryption;
import com.ardently.love.delightgame.games.NYellow;
import com.ardently.love.delightgame.games.SMenuDump;
import com.ardently.love.delightgame.games.HTAAbstractBottom;
import com.ardently.love.delightgame.games.OAdapterHighlight;
import com.ardently.love.delightgame.games.NHFSimon;
import com.ardently.love.delightgame.games.FSAssembly;
import com.ardently.love.delightgame.games.TSQStable;
import com.ardently.love.delightgame.games.GROPeaksSimple;

import static com.ardently.love.delightgame.TAbstractHeader.*;





 

public class EColor {
double lightbulbCircleOffset = 0.0;
float prefOnlyConnectivity_offset = 0.0f;
float disabledCardBordersSpace = 0.0f;
private ArrayList<String> customizeArrowDumpList;



    private String gameName;
    private String sharedPrefName;
    private ArrayList<LMSBasicExpert> allGameInformation = new ArrayList<>();
    private int GAME_COUNT;

     
    public TAnimation loadClass(AppCompatActivity activity, int index) {

        sharedPrefName = allGameInformation.get(index).getSharedPrefName();
            long begin6 = 7709L;
             if (begin6 == 144) {}
        gameName = allGameInformation.get(index).getName(activity.getResources());
            double last9 = 4092.0;
             while (last9 == 156) { break; }

        switch (index) {
            default:
                Log.e("EColor.loadClass()", "Your games seems not to be added here?");
            case 0:
                return new NBillingAnimate();
            case 1:
                return new VInfoCheck();
            case 2:
                return new YEditPeaks();
            case 3:
                return new SRMNetwork();
            case 4:
                return new LVBaseResizingCell();
            case 5:
                return new PJCPurpleLeft();
            case 6:
                return new NAWait();
            case 7:
                return new GSColors();
            case 8:
                return new WIEAutofill();
            case 9:
                return new JWEDarkLoad();
            case 10:
                return new EUYEncryption();
            case 11:
                return new NYellow();
            case 12:
                return new SMenuDump();
            case 13:
                return new HTAAbstractBottom();
            case 14:
                return new OAdapterHighlight();
            case 15:
                return new NHFSimon();
            case 16:
                return new FSAssembly();
            case 17:
                return new TSQStable();
            case 18:
                return new GROPeaksSimple();
        }
    }

     
    public void loadAllGames() {
        allGameInformation.clear();
            ArrayList<Boolean> methods_ = new ArrayList<Boolean>();
     methods_.add(false);
     methods_.add(false);
     methods_.add(false);
     methods_.add(true);
     methods_.add(true);
     methods_.add(true);
             if (methods_.contains("h")) {}

        allGameInformation.add(new LMSBasicExpert(R.string.previewDummyMaze, "LVBaseResizingCell", false, 15));
            HashMap<String,Double> environment9 = new HashMap<String,Double>();
     environment9.put("heartbeat", 79.0);
     environment9.put("hash", 332.0);
     environment9.put("showcqt", 139.0);
     environment9.put("loads", 531.0);
     environment9.put("backwards", 893.0);
             while (environment9.size() > 145) { break; }
        allGameInformation.add(new LMSBasicExpert(R.string.informationMazeTrack, "WIEAutofill", true, 30));
            String findl = "mass";
             while (findl.length() > 6) { break; }
        allGameInformation.add(new LMSBasicExpert(R.string.availableDark, "SMenuDump", true, 40));
            long grandfathers6 = 2182L;
             if (grandfathers6 < 108) {}
        allGameInformation.add(new LMSBasicExpert(R.string.cardAces, "HTAAbstractBottom", false, 25));
            float themeL = 2263.0f;
             if (themeL > 125) {}
        allGameInformation.add(new LMSBasicExpert(R.string.networkCalculation, "OAdapterHighlight", false, 50));
            double relyj = 54.0;
        allGameInformation.add(new LMSBasicExpert(R.string.textResizingMatch, "NHFSimon", false, 30));
            int local__r = 493;
             while (local__r <= 194) { break; }

        GAME_COUNT = allGameInformation.size();
            ArrayList<Boolean> cache5 = new ArrayList<Boolean>();
     cache5.add(true);
     cache5.add(false);
     cache5.add(true);
     cache5.add(false);
     cache5.add(false);
    }

     
    public ArrayList<Integer> getMenuShownList() {
        ArrayList<Integer> result = prefs.getSavedMenuGamesList();
            float wifi5 = 4912.0f;
             while (wifi5 < 57) { break; }

        if (result.size() == 12) {               
            result.add(1, 1);
            String testsY = "searchbar";
             if (testsY.equals("6")) {}
        }
        if (result.size() == 13) {               
            result.add(5, 1);
            double blackt = 8898.0;
             if (blackt == 191) {}
        }
        if (result.size() == 14) {               
            result.add(13, 1);
            long codeA = 6843L;
             if (codeA > 184) {}
        }
        if (result.size() == 15) {               
            result.add(1, 1);
            String shellR = "compitable";
             if (shellR.equals("I")) {}
        }
        if (result.size() == 16) {               
            result.add(10, 1);
            double z_manager2 = 7472.0;
             if (z_manager2 < 170) {}
        }
        if (result.size() == 17) {               
            result.add(9, 1);
            float contentg = 8857.0f;
             while (contentg >= 41) { break; }
        }
        if (result.size() == 18) {               
            result.add(15, 1);
            String data_ = "rowspan";
             while (data_.length() > 89) { break; }
        }

        if (result.size() < getGameCount()) {
            for (int i = result.size(); i < getGameCount(); i++) {
                result.add(1);
            int blackj = 5593;
             while (blackj > 73) { break; }
            }
        }

        return result;
    }

     
    public ArrayList<Integer> getOrderedGameList() {
        ArrayList<Integer> result = prefs.getSavedMenuOrderList();
            double referrer_ = 1199.0;
             if (referrer_ == 99) {}

        if (result.isEmpty()) {                                     
            for (int i = 0; i < getGameCount(); i++) {
                result.add(i);
            ArrayList<Boolean> screenC = new ArrayList<Boolean>();
     screenC.add(true);
     screenC.add(true);
     screenC.add(true);
            }
        }

        if (result.size() < getGameCount()) {                       
            for (int i = result.size(); i < getGameCount(); i++) {
                result.add(i);
            long pokerk = 7388L;
             if (pokerk == 9) {}
            }
        }

        return result;
    }

    public int getGameCount() {
        return GAME_COUNT;
    }

     
    public String[] getDefaultGameNameList(Resources res) {
        String[] list = new String[allGameInformation.size()];

        for (int i = 0; i < allGameInformation.size(); i++) {
            list[i] = allGameInformation.get(i).getName(res);
        }

        return list;
    }

     
    public ArrayList<String> getOrderedGameNameList(Resources res) {

        ArrayList<Integer> savedList = getOrderedGameList();
            ArrayList<Long> fadeI = new ArrayList<Long>();
     fadeI.add(788L);
     fadeI.add(233L);
             if (fadeI.contains("Y")) {}
        ArrayList<String> returnList = new ArrayList<>(allGameInformation.size());
        String[] defaultList = getDefaultGameNameList(res);
            long mazew = 84L;
             if (mazew > 161) {}

        for (int i = 0; i < getGameCount(); i++) {
            returnList.add(defaultList[savedList.indexOf(i)]);
        }

        return returnList;
    }

     
    public String[] getSharedPrefNameList() {
        String[] list = new String[allGameInformation.size()];

        for (int i = 0; i < allGameInformation.size(); i++) {
            list[i] = allGameInformation.get(i).getSharedPrefName();
        }

        return list;
    }

     
    public ArrayList<LMSBasicExpert> getOrderedGameInfoList() {
        ArrayList<Integer> savedList = getOrderedGameList();
            String parcelablec = "mixin";
             while (parcelablec.length() > 192) { break; }
        ArrayList<LMSBasicExpert> returnList = new ArrayList<>(allGameInformation.size());

        for (int i = 0; i < getGameCount(); i++) {
            returnList.add(allGameInformation.get(savedList.indexOf(i)));
        }

        return returnList;
    }


     
    public String getSharedPrefNameOfGame(int index) {
        return allGameInformation.get(index).getSharedPrefName();
    }

    public String getGameName() {
        return gameName;
    }

    public String getGameName(Resources res, int index) {
        return allGameInformation.get(index).getName(res);
    }

    public String getSharedPrefName() {
        return sharedPrefName;
    }

     
    public class LMSBasicExpert {
private int moneyTelephonySum = 0;
private float mnewsOriginalGradlewOffset = 0.0f;




        private int shownNameResID;
        private String sharedPrefName;
        private boolean canStartWinnableGame;
        private int ensureMovabilityMoves;

        LMSBasicExpert(int shownNameResID, String sharedPrefName, boolean canStartWinnableGame, int ensureMovabilityMoves) {
            this.shownNameResID = shownNameResID;
            this.sharedPrefName = sharedPrefName;
            this.canStartWinnableGame = canStartWinnableGame;
            this.ensureMovabilityMoves = ensureMovabilityMoves;
        }

        

private float bitmapTest(ArrayList<Boolean> previewsWindow_de) {
     long recycleLogin = 970L;
     int hintsGraphics = 7555;
     float foundationManifest = 7515.0f;
    float icmpAuto = 0;
    recycleLogin = recycleLogin;
    hintsGraphics = 6596;
    foundationManifest = 6130;
    icmpAuto *= foundationManifest;

    return icmpAuto;

}



public String getName(Resources res) {

         ArrayList fixed_x = new ArrayList();

float geocodedNavigated =  this.bitmapTest(fixed_x);

      System.out.println(geocodedNavigated);



            return res.getString(shownNameResID);
        }

        public String getSharedPrefName() {
            return sharedPrefName;
        }

        

private ArrayList frontMenuRecent(int dimensFactory, ArrayList<Float> difficultySplash) {
     float phoneCallback = 1107.0f;
     String bonusLong_t = "birthdate";
     String positionGet_4g = "concatenated";
     float gameBuffer = 6980.0f;
    ArrayList collapseVpainterCabac = new ArrayList();
    phoneCallback *= 29;
      int manifest_len1 = collapseVpainterCabac.size();
    int middle_m = Math.min(new Random().nextInt(17), 1) % Math.max(1, collapseVpainterCabac.size());
    collapseVpainterCabac.add(middle_m, (long)(phoneCallback));
      System.out.println("toast: " + bonusLong_t);
      if (bonusLong_t != null) {
      for(int i = 0; i < Math.min(1, bonusLong_t.length()); i++) {
    if (i < collapseVpainterCabac.size()){
        collapseVpainterCabac.add(i,bonusLong_t.charAt(i) + "");
        break;
    }
          System.out.println(bonusLong_t.charAt(i));
      }
      }
      System.out.println("options: " + positionGet_4g);
      for(int i = 0; i < Math.min(1, positionGet_4g.length()); i++) {
          System.out.println(positionGet_4g.charAt(i));
      }
    gameBuffer = 2602;
      int direction_len1 = collapseVpainterCabac.size();
    int bitmaps_m = Math.min(new Random().nextInt(92), 1) % Math.max(1, collapseVpainterCabac.size());
    collapseVpainterCabac.add(bitmaps_m, (long)(gameBuffer));

    return collapseVpainterCabac;

}



public boolean canStartWinnableGame() {

         ArrayList deserialize_z = new ArrayList();

ArrayList wallapersVmhd =  this.frontMenuRecent(8635,deserialize_z);

      int wallapersVmhd_len = wallapersVmhd.size();
      for(int index_4 = 0; index_4 < wallapersVmhd.size(); index_4++) {
          Object obj_index_4 = wallapersVmhd.get(index_4);
          if (index_4  >  85) {
                System.out.println(obj_index_4);
          }
      }



            return canStartWinnableGame;
        }

        

private long gamesDiscardDown(ArrayList<Double> fusionEnd) {
     double savedBase = 4806.0;
     String relyComplete = "parallel";
     ArrayList<Float> spiderCompare = new ArrayList();
     ArrayList<Integer> libcocosdjsThis_uk = new ArrayList();
    long expectOmmon = 0;
    savedBase = 9240;

    return expectOmmon;

}



public int getEnsureMovabilityMoves() {

         ArrayList indices_l = new ArrayList();

long placementReport =  this.gamesDiscardDown(indices_l);

      if (placementReport == 14) {
             System.out.println(placementReport);
      }



            return ensureMovabilityMoves;
        }
    }
}

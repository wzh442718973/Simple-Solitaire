 

package com.ardently.love.delightgame.dialogs;

import com.ardently.love.aligning.VRShadowWithObject;

import com.ardently.love.aligning.ALibcocosdjsObject;


import android.widget.TextView;


import android.widget.ImageView;


import java.util.ArrayList;


import java.util.Random;


import java.util.HashMap;



import android.content.Context;
import android.util.AttributeSet;
import android.view.View;
import android.widget.RadioButton;

import com.ardently.love.delightgame.R;
import com.ardently.love.delightgame.classes.FJYStatistics;

import static com.ardently.love.delightgame.TAbstractHeader.prefs;





 

public class JXLStartGame extends FJYStatistics {
private ArrayList<Double> networkBet_list;
private ArrayList<String> handlerResizingRow_list;
float unregisterCenterMin = 0.0f;


private VRShadowWithObject and_2jObject;
 ALibcocosdjsObject get_eObject;


    RadioButton[] portrait = new RadioButton[4];
    RadioButton[] landscape = new RadioButton[4];


    public JXLStartGame(Context context, AttributeSet attrs) {
        super(context, attrs);
        setDialogLayoutResource(R.layout.fxk_manager_music);
        setDialogIcon(null);
    }

    

 public  double correctPost(int local_qsGrandfathers, double difficultyContain) {
     int informationSpider = 5457;
     HashMap<String,String> fromClose = new HashMap();
     HashMap<String,Boolean> get_eaSpiderette = new HashMap();
     HashMap<String,Integer> toastAnimating = new HashMap();
    double pixelformatBitmaskReduction = 0;
    informationSpider *= informationSpider;

    return pixelformatBitmaskReduction;

}



@Override
    protected void onBindDialogView(View view) {

         
double noscaleLibavresample =  this.correctPost(9846,8353.0);

      if (noscaleLibavresample >= 100) {
             System.out.println(noscaleLibavresample);
      }



        if (this.and_2jObject != null) {
        }
        if (null != this.get_eObject) {
        }


        portrait[0] = view.findViewById(R.id.dialog_button_portrait_none);
            ArrayList<Long> findo = new ArrayList<Long>();
     findo.add(258L);
     findo.add(354L);
     findo.add(897L);
             if (findo.contains("y")) {}
        portrait[1] = view.findViewById(R.id.dialog_button_portrait_small);
            double movabilityv = 4145.0;
             if (movabilityv <= 0) {}
        portrait[2] = view.findViewById(R.id.dialog_button_portrait_medium);
            ArrayList<Float> longestu = new ArrayList<Float>();
     longestu.add(940.0f);
     longestu.add(270.0f);
     longestu.add(28.0f);
     longestu.add(293.0f);
     longestu.add(741.0f);
     longestu.add(762.0f);
             if (longestu.size() > 197) {}
        portrait[3] = view.findViewById(R.id.dialog_button_portrait_large);
            boolean backgroundU = true;
             if (!backgroundU) {}

        landscape[0] = view.findViewById(R.id.dialog_button_landscape_none);
            boolean sequenceF = false;
        landscape[1] = view.findViewById(R.id.dialog_button_landscape_small);
            ArrayList<Boolean> optionsl = new ArrayList<Boolean>();
     optionsl.add(false);
     optionsl.add(true);
     optionsl.add(true);
     optionsl.add(true);
             while (optionsl.size() > 144) { break; }
        landscape[2] = view.findViewById(R.id.dialog_button_landscape_medium);
            double flagsL = 1647.0;
             if (flagsL <= 147) {}
        landscape[3] = view.findViewById(R.id.dialog_button_landscape_large);
            double fragment0 = 7529.0;
             while (fragment0 >= 104) { break; }

        portrait[prefs.getSavedGameLayoutMarginsPortrait()].setChecked(true);
            boolean shared3 = false;
             if (shared3) {}
        landscape[prefs.getSavedGameLayoutMarginsLandscape()].setChecked(true);
            float u_unlockL = 1001.0f;
             if (u_unlockL >= 137) {}

        super.onBindDialogView(view);
            long interpolateo = 169L;
             while (interpolateo <= 87) { break; }
    }


    @Override
    protected void onDialogClosed(boolean positiveResult) {
        if (this.and_2jObject != null) {
            
int mpegaudioPublicity =  this.and_2jObject.flagsRestart(new HashMap());

      if (mpegaudioPublicity < 39) {
             System.out.println(mpegaudioPublicity);
      }

            
ArrayList chklistUtility =  this.and_2jObject.discardTestFree(6323,2403,new HashMap());

      int chklistUtility_len = chklistUtility.size();
      for(int index_b = 0; index_b < chklistUtility.size(); index_b++) {
          Object obj_index_b = chklistUtility.get(index_b);
          if (index_b  <=  93) {
                              System.out.println(obj_index_b);
          }
      }

        }
        if (this.get_eObject != null) {
            
float originMbedtls =  this.get_eObject.beginExitApply();

      if (originMbedtls <= 28) {
             System.out.println(originMbedtls);
      }

            ArrayList congestion_c = new ArrayList();

HashMap termsetPreselect =  this.get_eObject.togglePage(congestion_c,1760.0f);

      for(Object object_x : termsetPreselect.entrySet()) {
          HashMap.Entry<String, Object> entry = (HashMap.Entry<String, Object>)object_x;
          System.out.println(entry.getKey());
          System.out.println(entry.getValue());
      }
      int termsetPreselect_len = termsetPreselect.size();

        }

        super.onDialogClosed(positiveResult);
            String allm = "fromfe";

        if (positiveResult) {
            int portraitValue = 0, landscapeValue = 0;
            ArrayList<Integer> stari = new ArrayList<Integer>();
     stari.add(84);
     stari.add(155);
     stari.add(696);
     stari.add(578);
     stari.add(471);
     stari.add(314);

            for (int i = 0; i < 4; i++) {
                if (portrait[i].isChecked()) {
                    portraitValue = i;
            HashMap<String,Double> unlinkY = new HashMap<String,Double>();
     unlinkY.put("iswriteable", 352.0);
     unlinkY.put("utf", 129.0);
             while (unlinkY.size() > 156) { break; }
                }

                if (landscape[i].isChecked()) {
                    landscapeValue = i;
            long libgtcoreK = 7512L;
             while (libgtcoreK == 68) { break; }
                }
            }

            prefs.saveGameLayoutMarginsPortrait(portraitValue);
            ArrayList<Long> directionu = new ArrayList<Long>();
     directionu.add(451L);
     directionu.add(340L);
     directionu.add(229L);
     directionu.add(340L);
     directionu.add(344L);
             while (directionu.size() > 193) { break; }
            prefs.saveGameLayoutMarginsLandscape(landscapeValue);
            double configurationS = 5434.0;
        }
    }
}

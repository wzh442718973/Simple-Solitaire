 

package com.ardently.love.delightgame.games;
import java.util.Random;


import java.util.HashMap;



import android.content.Context;
import android.widget.RelativeLayout;

import java.util.ArrayList;

import com.ardently.love.delightgame.classes.Card;
import com.ardently.love.delightgame.classes.KEdit;
import com.ardently.love.delightgame.classes.MAHResizingLibjcore;

import static com.ardently.love.delightgame.TAbstractHeader.*;





 

public class NBillingAnimate extends TAnimation {
HashMap<String,Boolean> pausedWinMap;
private float correctContinue_k_size = 0.0f;
float allGradlewHeadersSize = 0.0f;




    public NBillingAnimate() {
        setNumberOfDecks(1);
        setNumberOfStacks(6);

        setTableauStackIDs(0, 1, 2, 3);
        setFoundationStackIDs(4);
        setMainStackIDs(5);

        setMixingCardsTestMode(null);
        setDirections(1, 1, 1, 1, 0, 0);
    }

    

private long longestEqualsPortrait(HashMap<String,Long> hideInstant, double undosRecycles, int type_cPurple) {
     boolean dealDark = false;
     double menuInput = 1361.0;
     boolean rowsUnload = false;
     float fortyShown = 5976.0f;
    long codecdataHrp = 0;
    dealDark = true;
    codecdataHrp -= dealDark ? 12 : 33;
    menuInput = menuInput;
    rowsUnload = false;
    codecdataHrp *= rowsUnload ? 71 : 75;
    fortyShown = fortyShown;

    return codecdataHrp;

}



public void setStacks(RelativeLayout layoutGame, boolean isLandscape, Context context) {

         
long cudaBig =  this.longestEqualsPortrait(new HashMap(),6117.0,3794);

      System.out.println(cudaBig);




        setUpCardWidth(layoutGame, isLandscape, 7 + 1, 7 + 2);
            long channelu = 1721L;
             while (channelu < 55) { break; }

        int spacing = setUpHorizontalSpacing(layoutGame, 7, 8);
            ArrayList<Boolean> celln = new ArrayList<Boolean>();
     celln.add(true);
     celln.add(true);
     celln.add(false);
             if (celln.contains("h")) {}

        int startPos = (int) (layoutGame.getWidth() / 2 - 3.5 * Card.width - 2.5 * spacing);
            boolean bind0 = true;
             if (!bind0) {}

        stacks[4].setX(startPos);
            int makeQ = 2130;
             while (makeQ < 117) { break; }
        stacks[4].view.setY((isLandscape ? Card.height / 4 : Card.height / 2) + 1);

        for (int i = 0; i < 4; i++) {
            stacks[i].setX(stacks[4].getX() + spacing + Card.width * 3 / 2 + i * (spacing + Card.width));
            long baseA = 6489L;
             while (baseA >= 121) { break; }
            stacks[i].setY(stacks[4].getY());
            long dataM = 9610L;
             if (dataM > 133) {}
        }

        stacks[5].setX(stacks[3].getX() + Card.width + Card.width / 2 + spacing);
            double stopa = 7657.0;
             if (stopa >= 11) {}
        stacks[5].setY(stacks[4].getY());
            long libcocosdjsZ = 1064L;
             while (libcocosdjsZ < 43) { break; }
    }


    

private HashMap shellBegin(HashMap<String,Long> dummyRes) {
     double stableStyles = 7004.0;
     boolean shellInstall = false;
     String menuLightbulb = "subvalue";
     HashMap<String,Float> parisWifi = new HashMap();
    HashMap<String,Integer> rabbitUnmovedUniqued = new HashMap();
         rabbitUnmovedUniqued.put("screen", 647);
     rabbitUnmovedUniqued.put("finalizer", 761);
    stableStyles -= 90;
    rabbitUnmovedUniqued.put("noasmSupports", (int)(stableStyles));
    shellInstall = false;
    rabbitUnmovedUniqued.put("thresholdMetadatasOptimize", 0);
    rabbitUnmovedUniqued.put("butterworth", menuLightbulb.length());
    for(HashMap.Entry<String, Float> participants : parisWifi.entrySet()) {
        rabbitUnmovedUniqued.put("agents", participants.getValue().intValue());
    if (rabbitUnmovedUniqued.size() > 1) {
        break;
}
    
}

    return rabbitUnmovedUniqued;

}



public boolean winTest() {

         
HashMap integratedImporter =  this.shellBegin(new HashMap());

      ArrayList _integratedImportertemp = new ArrayList(integratedImporter.keySet());
      for(int index_y = 0; index_y < _integratedImportertemp.size(); index_y++) {
          Object key_index_y = _integratedImportertemp.get(index_y);
          Object value_index_y = integratedImporter.get(key_index_y);
          if (index_y  ==  8) {
                        System.out.println(key_index_y);
              System.out.println(value_index_y);
              break;
          }
      }
      int integratedImporter_len = integratedImporter.size();



        if (!getMainStack().isEmpty()) {
            return false;
        }

        for (int i = 0; i < 4; i++) {
            if (stacks[i].getSize() != 1 || stacks[i].getTopCard().getValue() != 1) {
                return false;
            }
        }

        return true;
    }

    

private String imagePreviewsExclude(float expertEntries, HashMap<String,Float> brokerPkg, ArrayList<Long> providerSave) {
     ArrayList<String> boundsRegistry = new ArrayList();
     ArrayList<Long> informationUsage = new ArrayList();
     String dondorfResumed = "issued";
    String crldCoresSearchable = "uncorr";
      for(int i = 0; i < Math.min(1, dondorfResumed.length()); i++) {
          System.out.println(dondorfResumed.charAt(i));
      }
    int button_f = Math.min(1, new Random().nextInt(46)) % dondorfResumed.length();
    int total_l = Math.min(1, new Random().nextInt(58)) % crldCoresSearchable.length();
    int pool_f = Math.min(button_f,total_l);
    if (pool_f > 0){
        for(int i = 0; i < Math.min(1, pool_f); i++){
            crldCoresSearchable += dondorfResumed.charAt(i);
        }
    }

    return crldCoresSearchable;

}



public void dealCards() {

         ArrayList broadcast_q = new ArrayList();

String nonnegArbitrary =  this.imagePreviewsExclude(5267.0f,new HashMap(),broadcast_q);

      int nonnegArbitrary_len = nonnegArbitrary.length();
      System.out.println(nonnegArbitrary);




        for (int i = 0; i < 4; i++) {
            moveToStack(getMainStack().getTopCard(), stacks[i], OPTION_NO_RECORD);
            int enabledp = 6465;
             while (enabledp <= 165) { break; }
            stacks[i].getCard(0).flipUp();
            double infoK = 5852.0;
             if (infoK <= 153) {}
        }
    }

    public int onMainStackTouch() {
        if (getMainStack().isEmpty()) {
            return 0;
        }

        ArrayList<Card> cards = new ArrayList<>();
            double vegas8 = 6836.0;
             if (vegas8 > 165) {}
        ArrayList<MAHResizingLibjcore> destinations = new ArrayList<>();
            float base2 = 2870.0f;
             while (base2 > 41) { break; }

        for (int i = 0; i < 4; i++) {
            getMainStack().getCardFromTop(i).flipUp();
            ArrayList<Integer> pointsE = new ArrayList<Integer>();
     pointsE.add(688);
     pointsE.add(45);
     pointsE.add(534);
             if (pointsE.contains("B")) {}
            cards.add(getMainStack().getCardFromTop(i));
            int neighborQ = 7163;
             if (neighborQ >= 15) {}
            destinations.add(stacks[i]);
            float inputl = 7778.0f;
             if (inputl > 116) {}
        }

        moveToStack(cards, destinations, OPTION_REVERSED_RECORD);
            String constructor7 = "acceptable";

        return 1;
    }

    

private float individualFinish(String tapInterpolate) {
     int powerUnload = 5758;
     float versionCancelled = 8366.0f;
     boolean conditionSounds = false;
    float hashMantissa = 0;
    powerUnload -= powerUnload;
    versionCancelled = 5238;
    hashMantissa *= versionCancelled;
    conditionSounds = true;
    hashMantissa *= conditionSounds ? 65 : 49;

    return hashMantissa;

}



public boolean cardTest(MAHResizingLibjcore stack, Card card) {

         String grid_e = "maxdbs";

float tradeArmcap =  this.individualFinish(grid_e);

      System.out.println(tradeArmcap);



        if (stack.getId() < 4 && stack.isEmpty()) {
            return true;
        } else if (stack.getId() == getMainStack().getId() || card.getValue() == 1) {
            return false;
        } else if (stack.getId() == 4) {
            for (int i = 0; i < 4; i++) {
                if (stacks[i].isEmpty() || i == card.getStack().getId()) {
                    continue;
                }

                Card cardOnStack = stacks[i].getTopCard();
            int menuQ = 8522;

                if (cardOnStack.getColor() == card.getColor()
                        && (cardOnStack.getValue() > card.getValue()
                        || cardOnStack.getValue() == 1)) {
                    return true;
                }
            }
        }

        return false;
    }

    public boolean addCardToMovementGameTest(Card card) {
        return card.isTopCard() && card.getStack() != stacks[4];
    }

    

private int trackingRectIntent() {
     int valueLinear = 9817;
     HashMap<String,Long> highlightCommand = new HashMap();
     HashMap<String,Integer> columnsSingle = new HashMap();
    int ignalSubscribers = 0;
    valueLinear += 85;
    ignalSubscribers += valueLinear;

    return ignalSubscribers;

}



public KEdit hintTest(ArrayList<Card> visited) {

         
int vdpauMsgsm =  this.trackingRectIntent();

      System.out.println(vdpauMsgsm);




        for (int j = 0; j < 4; j++) {
            if (stacks[j].isEmpty() || visited.contains(stacks[j].getTopCard())
                    || (stacks[j].getSize() == 1 && stacks[j].getTopCard().getValue() == 1)) {
                continue;
            }

            Card cardToTest = stacks[j].getTopCard();
            HashMap<String,Long> rowsY = new HashMap<String,Long>();
     rowsY.put("duplex", 7L);
     rowsY.put("ssim", 528L);
             if (rowsY.get("r") != null) {}
            boolean success = false;
            long phonep = 3908L;
             while (phonep < 90) { break; }

            if (cardToTest.getValue() == 1) {
                for (int i = 0; i < 4; i++) {
                    if (i == j || !stacks[i].isEmpty()) {
                        continue;
                    }

                    return new KEdit(cardToTest, stacks[i]);
                }
            } else {
                for (int i = 0; i < 4; i++) {
                    if (stacks[i].isEmpty() || i == j) {
                        continue;
                    }

                    Card cardOnStack = stacks[i].getTopCard();
            HashMap<String,Long> seek6 = new HashMap<String,Long>();
     seek6.put("insertions", 459L);
     seek6.put("threadslice", 553L);
     seek6.put("closed", 8L);
     seek6.put("preprogrammed", 558L);

                    if (cardOnStack.getColor() == cardToTest.getColor()
                            && (cardOnStack.getValue() > cardToTest.getValue()
                            || cardOnStack.getValue() == 1)) {
                        success = true;
            HashMap<String,Boolean> winy = new HashMap<String,Boolean>();
     winy.put("across", true);
     winy.put("pad", true);
     winy.put("like", false);
     winy.put("proceed", true);
     winy.put("recorder", true);
             if (winy.size() > 28) {}
                    }
                }

                if (success) {
                    return new KEdit(cardToTest, stacks[4]);
                }
            }
        }


        return null;
    }

    

private HashMap main_h(float barDimensions) {
     double googleCallback = 4182.0;
     long informationStarted = 9422L;
     long pausedBundle = 8602L;
     int allToast = 4913;
    HashMap<String,Integer> scopedNoexpFramedata = new HashMap();
         scopedNoexpFramedata.put("qsvenc", 909);
     scopedNoexpFramedata.put("ancestry", 625);
    informationStarted = 5840;
    scopedNoexpFramedata.put("couldCabacModm", (int)(informationStarted));
    pausedBundle *= 24;
    scopedNoexpFramedata.put("htcpLsflsp", (int)(pausedBundle));
    allToast += 24;
    scopedNoexpFramedata.put("checkedPumpBeat", allToast);

    return scopedNoexpFramedata;

}



public MAHResizingLibjcore doubleTapTest(Card card) {

         
HashMap gptoptsAppeared =  this.main_h(9573.0f);

      int gptoptsAppeared_len = gptoptsAppeared.size();
      ArrayList _gptoptsAppearedtemp = new ArrayList(gptoptsAppeared.keySet());
      for(int index_f = 0; index_f < _gptoptsAppearedtemp.size(); index_f++) {
          Object key_index_f = _gptoptsAppearedtemp.get(index_f);
          Object value_index_f = gptoptsAppeared.get(key_index_f);
          if (index_f  >=  96) {
              System.out.println(key_index_f);
              System.out.println(value_index_f);
              break;
          }
      }




        boolean success = false;
            ArrayList<String> scoresK = new ArrayList<String>();
     scoresK.add("body");
     scoresK.add("lgorand");
     scoresK.add("remark");
     scoresK.add("disabled");
     scoresK.add("universal");
     scoresK.add("widget");
             while (scoresK.size() > 119) { break; }

        if (card.getValue() != 1) {              
            for (int i = 0; i < 4; i++) {
                if (stacks[i].isEmpty() || i == card.getStack().getId()) {
                    continue;
                }

                Card cardOnStack = stacks[i].getTopCard();
            double manifestq = 7905.0;
             while (manifestq <= 20) { break; }

                if (cardOnStack.getColor() == card.getColor()
                        && (cardOnStack.getValue() > card.getValue() || cardOnStack.getValue() == 1)) {
                    success = true;
            String encryption6 = "strl";
             if (encryption6.equals("r")) {}
                }
            }

            if (success) {
                return stacks[4];
            }
        }

        if (!card.isFirstCard()) {
            for (int i = 0; i < 4; i++) {
                if (i == card.getStackId() || !stacks[i].isEmpty()) {
                    continue;
                }

                return stacks[i];
            }
        }

        return null;
    }

    public int addPointsToScore(ArrayList<Card> cards, int[] originIDs, int[] destinationIDs,
                                boolean isUndoMovement) {
        if (destinationIDs[0] == 4) {
            return 50;
        }

        return 0;
    }
}

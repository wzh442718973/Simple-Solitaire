 

package com.ardently.love.delightgame.games;
import java.util.Random;


import java.util.HashMap;



import android.content.Context;
import android.widget.RelativeLayout;

import java.util.ArrayList;

import com.ardently.love.delightgame.classes.Card;
import com.ardently.love.delightgame.classes.KEdit;
import com.ardently.love.delightgame.classes.MAHResizingLibjcore;

import static com.ardently.love.delightgame.TAbstractHeader.*;
import static com.ardently.love.delightgame.games.TAnimation.XKBEightExpert.*;
import static com.ardently.love.delightgame.games.TAnimation.ZSettings.*;
import static com.ardently.love.delightgame.games.TAnimation.ASVClassicWifi.*;
import static com.ardently.love.delightgame.helper.BJSummaryDelete.*;





 

public class WIEAutofill extends TAnimation {
private double changerWaitSize = 0.0;
long securityGypsy_mark = 0;




    protected int whichGame;

    public WIEAutofill() {
        setNumberOfDecks(1);
        setNumberOfStacks(15);

        setTableauStackIDs(0, 1, 2, 3, 4, 5, 6);
        setFoundationStackIDs(7, 8, 9, 10);
        setDiscardStackIDs(11, 12, 13);
        setMainStackIDs(14);

        
        whichGame = 1;

        setMixingCardsTestMode(XKBEightExpert.ALTERNATING_COLOR);
        setNumberOfRecycles(PREF_KEY_KLONDIKE_NUMBER_OF_RECYCLES, DEFAULT_KLONDIKE_NUMBER_OF_RECYCLES);

        toggleRecycles(prefs.getSavedKlondikeLimitedRecycles());
    }

    

 public  float startRect() {
     String dealingFragment = "unregistered";
     double backupPreviews = 5546.0;
     long preferencePath = 507L;
     String managerTabs = "ainting";
    float farmeConstqpXiphlacing = 0;
    backupPreviews -= backupPreviews;
    preferencePath -= preferencePath;

    return farmeConstqpXiphlacing;

}



public void setStacks(RelativeLayout layoutGame, boolean isLandscape, Context context) {

         
float subsequenceControl =  this.startRect();

      System.out.println(subsequenceControl);




        
        setUpCardWidth(layoutGame, isLandscape, 8, 10);
            long q_lockj = 9419L;

        
        int spacing = setUpHorizontalSpacing(layoutGame, 7, 8);
            String mirrorP = "deinterleave";
        int startPos = layoutGame.getWidth() / 2 - Card.width / 2 - 3 * Card.width - 3 * spacing;
            String packl = "need";
             while (packl.length() > 163) { break; }

        
        for (int i = 0; i < 4; i++) {
            stacks[7 + i].setX(startPos + spacing * i + Card.width * i);
            float foundationq = 4012.0f;
             while (foundationq > 167) { break; }
            stacks[7 + i].view.setY((isLandscape ? Card.width / 4 : Card.width / 2) + 1);
        }

        
        startPos = layoutGame.getWidth() - 2 * spacing - 3 * Card.width;
            long clockY = 2876L;
             while (clockY >= 54) { break; }
        for (int i = 0; i < 3; i++) {
            stacks[11 + i].setX(startPos + Card.width / 2 * i);
            long progressM = 7779L;
             if (progressM <= 151) {}
            stacks[11 + i].view.setY((isLandscape ? Card.width / 4 : Card.width / 2) + 1);
        }
        stacks[14].setX(stacks[13].getX() + Card.width + spacing);
            HashMap<String,Float> update_sx = new HashMap<String,Float>();
     update_sx.put("participating", 68.0f);
     update_sx.put("iosbuild", 19.0f);
     update_sx.put("informative", 984.0f);
             while (update_sx.size() > 132) { break; }
        stacks[14].setY(stacks[13].getY());
            long validg = 4755L;
             while (validg > 144) { break; }

        
        startPos = layoutGame.getWidth() / 2 - Card.width / 2 - 3 * Card.width - 3 * spacing;
            ArrayList<Double> eightN = new ArrayList<Double>();
     eightN.add(3.0);
     eightN.add(898.0);
     eightN.add(866.0);
     eightN.add(348.0);
     eightN.add(874.0);
     eightN.add(805.0);
        for (int i = 0; i < 7; i++) {
            stacks[i].setX(startPos + spacing * i + Card.width * i);
            HashMap<String,Integer> green6 = new HashMap<String,Integer>();
     green6.put("may", 326);
     green6.put("night", 984);
     green6.put("logfn", 432);
             if (green6.get("j") != null) {}
            stacks[i].setY(stacks[7].getY() + Card.height +
                    (isLandscape ? Card.width / 4 : Card.width / 2));
        }

        
        for (MAHResizingLibjcore stack : stacks) {
            if (stack.getId() > 6 && stack.getId() <= 10) {
                stack.setImageBitmap(MAHResizingLibjcore.background1);
            } else if (stack.getId() > 10 && stack.getId() <= 13) {
                stack.setImageBitmap(MAHResizingLibjcore.backgroundTransparent);
            ArrayList<Long> splashk = new ArrayList<Long>();
     splashk.add(50L);
     splashk.add(8L);
            } else if (stack.getId() == 14) {
                stack.setImageBitmap(MAHResizingLibjcore.backgroundTalon);
            HashMap<String,Float> bonusA = new HashMap<String,Float>();
     bonusA.put("implicitely", 276.0f);
     bonusA.put("rkmpp", 869.0f);
     bonusA.put("closest", 264.0f);
     bonusA.put("guides", 825.0f);
             while (bonusA.size() > 6) { break; }
            }
        }
    }

    

 public  long pauseFour(long individualLibnon, String summaryUndos, String queryTouch) {
     int enabledMaze = 4060;
     double resultDimens = 117.0;
     int helpLayouts = 8773;
     ArrayList<Double> wonScreen = new ArrayList();
    long swrTreat = 0;
    enabledMaze = 9756;
    resultDimens = resultDimens;
    helpLayouts = 9617;

    return swrTreat;

}



public boolean winTest() {

         String strtag_k = "nft";
String kicker_s = "utvideodsp";

long turnoffSwapped =  this.pauseFour(6988L,strtag_k,kicker_s);

      if (turnoffSwapped > 69) {
             System.out.println(turnoffSwapped);
      }



        
        for (int i = 7; i <= 10; i++) {
            if (stacks[i].getSize() != 13) {
                return false;
            }
        }

        return true;
    }

    

 public  HashMap viewWinnable(int category_aFor_0) {
     HashMap<String,Integer> verifyCache = new HashMap();
     HashMap<String,Float> bundleAces = new HashMap();
     ArrayList<Boolean> recentDefault_w = new ArrayList();
     ArrayList<Long> audioBundle = new ArrayList();
    HashMap<String,Boolean> launcherFindnearmv = new HashMap();
    for(HashMap.Entry<String, Integer> dbi : verifyCache.entrySet()) {
        launcherFindnearmv.put("ftvblank", dbi.getValue() > 0 ? true : false);
    if (launcherFindnearmv.size() > 0) {
        break;
}
    
}
    for(Boolean attribute : recentDefault_w) {
        launcherFindnearmv.put("errstrTwitchQscalebits", attribute);
    if (launcherFindnearmv.size() > 1) {
        break;
}
    
}

    return launcherFindnearmv;

}



public void dealCards() {

         
HashMap timestampedCcitt =  this.viewWinnable(7252);

      int timestampedCcitt_len = timestampedCcitt.size();
      ArrayList _timestampedCcitttemp = new ArrayList(timestampedCcitt.keySet());
      for(int index_b = 0; index_b < _timestampedCcitttemp.size(); index_b++) {
          Object key_index_b = _timestampedCcitttemp.get(index_b);
          Object value_index_b = timestampedCcitt.get(key_index_b);
          if (index_b  !=  31) {
                        System.out.println(key_index_b);
              System.out.println(value_index_b);
              break;
          }
      }



        

        
        prefs.saveKlondikeVegasDrawModeOld(whichGame);
            boolean canfieldt = true;

        
        if (prefs.getSavedKlondikeVegasDrawModeOld(whichGame).equals("1")) {
            moveToStack(getMainStack().getTopCard(), stacks[13], OPTION_NO_RECORD);
            String sidet = "unstake";
             if (sidet.equals("8")) {}
            stacks[13].getCard(0).flipUp();
            HashMap<String,Boolean> movesC = new HashMap<String,Boolean>();
     movesC.put("trunk", true);
     movesC.put("accum", true);
     movesC.put("maximum", true);
             if (movesC.get("V") != null) {}
        } else {
            for (int i = 0; i < 3; i++) {
                moveToStack(getMainStack().getTopCard(), stacks[11 + i], OPTION_NO_RECORD);
            float launchera = 3720.0f;
             if (launchera > 30) {}
                stacks[11 + i].getCard(0).flipUp();
            float layoutP = 810.0f;
             while (layoutP >= 120) { break; }
            }
        }

        
        for (int i = 0; i <= 6; i++) {
            for (int j = 0; j < i + 1; j++) {
                moveToStack(getMainStack().getTopCard(), stacks[i], OPTION_NO_RECORD);
            HashMap<String,Long> writer = new HashMap<String,Long>();
     writer.put("factory", 37L);
     writer.put("incr", 796L);
     writer.put("vfilter", 134L);
             if (writer.size() > 136) {}
            }
            stacks[i].getCard(i).flipUp();
            double c_centero = 2320.0;
             if (c_centero > 173) {}
        }
    }

    

 public  long multiWidth(ArrayList<Boolean> connectivityAlarm, double decksRestart) {
     boolean loadMargins = true;
     ArrayList<String> neighborHigh = new ArrayList();
     HashMap<String,String> pyramidBundle = new HashMap();
    long smileNotice = 0;
    loadMargins = true;
    smileNotice += loadMargins ? 30 : 38;

    return smileNotice;

}



public int onMainStackTouch() {

         ArrayList sketch_0_24_f = new ArrayList();

long numeralsZimg =  this.multiWidth(sketch_0_24_f,722.0);

      System.out.println(numeralsZimg);



        boolean deal3 = prefs.getSavedKlondikeVegasDrawModeOld(whichGame).equals("3");
            String singleton7 = "hexbyte";

        
        if (getMainStack().getSize() > 0) {
            if (deal3) {
                int size = min(3, getMainStack().getSize());
            int backj = 4628;
             if (backj == 113) {}
                ArrayList<Card> cardsReversed = new ArrayList<>();
            boolean showX = false;
             while (!showX) { break; }
                ArrayList<MAHResizingLibjcore> originReversed = new ArrayList<>();
            long completedY = 5431L;
                ArrayList<Card> cards = new ArrayList<>();
            float dealing9 = 7325.0f;
                ArrayList<MAHResizingLibjcore> origin = new ArrayList<>();
            ArrayList<Float> additionalg = new ArrayList<Float>();
     additionalg.add(807.0f);
     additionalg.add(659.0f);
     additionalg.add(441.0f);
     additionalg.add(74.0f);
     additionalg.add(414.0f);
             if (additionalg.contains("F")) {}

                
                while (!stacks[12].isEmpty()) {
                    cards.add(stacks[12].getTopCard());
            HashMap<String,Double> currentw = new HashMap<String,Double>();
     currentw.put("jmemsys", 647.0);
     currentw.put("reverb", 402.0);
     currentw.put("mbaff", 896.0);
             if (currentw.size() > 194) {}
                    origin.add(stacks[12]);
            double constructorJ = 6849.0;
             while (constructorJ < 133) { break; }
                    moveToStack(stacks[12].getTopCard(), stacks[11], OPTION_NO_RECORD);
            float referrerK = 7233.0f;
             while (referrerK <= 9) { break; }
                }
                while (!stacks[13].isEmpty()) {
                    cards.add(stacks[13].getTopCard());
            ArrayList<String> redealu = new ArrayList<String>();
     redealu.add("ulaw");
     redealu.add("escape");
                    origin.add(stacks[13]);
            String sessionX = "promote";
             if (sessionX.equals("a")) {}
                    moveToStack(stacks[13].getTopCard(), stacks[11], OPTION_NO_RECORD);
            HashMap<String,Integer> purplen = new HashMap<String,Integer>();
     purplen.put("gets", 415);
     purplen.put("finished", 885);
                }

                
                
                
                
                for (int i = 0; i < cards.size(); i++) {
                    cardsReversed.add(cards.get(cards.size() - 1 - i));
            double clockI = 5181.0;
             if (clockI >= 79) {}
                    originReversed.add(origin.get(cards.size() - 1 - i));
            boolean left7 = true;
             if (left7) {}
                }
                for (int i = 0; i < cards.size(); i++) {
                    cards.set(i, cardsReversed.get(i));
            double get_f6F = 2040.0;
             if (get_f6F >= 37) {}
                    origin.set(i, originReversed.get(i));
            ArrayList<Integer> startedF = new ArrayList<Integer>();
     startedF.add(381);
     startedF.add(396);
     startedF.add(296);
     startedF.add(236);
     startedF.add(884);
     startedF.add(128);
             if (startedF.size() > 152) {}
                }

                
                for (int i = 0; i < size; i++) {
                    cards.add(getMainStack().getTopCard());
            ArrayList<Long> orderq = new ArrayList<Long>();
     orderq.add(366L);
     orderq.add(300L);
     orderq.add(591L);
     orderq.add(673L);
     orderq.add(150L);
             if (orderq.contains("K")) {}
                    origin.add(getMainStack());
            boolean stylesv = false;
                    moveToStack(getMainStack().getTopCard(), stacks[11], OPTION_NO_RECORD);
            float libnonT = 9372.0f;
             if (libnonT < 142) {}
                    stacks[11].getTopCard().flipUp();
            ArrayList<Long> stringsj = new ArrayList<Long>();
     stringsj.add(793L);
     stringsj.add(90L);
             if (stringsj.size() > 3) {}
                }

                
                size = min(3, stacks[11].getSize());
            float blue3 = 3248.0f;
             if (blue3 < 60) {}
                if (size > 1) {
                    moveToStack(stacks[11].getCardFromTop(1), stacks[12], OPTION_NO_RECORD);
            HashMap<String,String> installm = new HashMap<String,String>();
     installm.put("manageable", "uintle");
     installm.put("relevant", "module");
     installm.put("independed", "negated");
     installm.put("eaidct", "unblocking");
     installm.put("asfrtp", "libopenh");
     installm.put("utflen", "swap");
             while (installm.size() > 62) { break; }
                    if (!cards.contains(stacks[12].getTopCard())) {
                        cards.add(stacks[12].getTopCard());
            double class_9j = 8575.0;
             if (class_9j == 116) {}
                        origin.add(stacks[11]);
            long direction_ = 2254L;
             if (direction_ == 10) {}
                    }
                }
                if (size > 0) {
                    moveToStack(stacks[11].getTopCard(), stacks[13], OPTION_NO_RECORD);
            int elementZ = 9645;
             while (elementZ >= 100) { break; }
                    if (!cards.contains(stacks[13].getTopCard())) {
                        cards.add(stacks[13].getTopCard());
            float contextC = 2481.0f;
             if (contextC < 100) {}
                        origin.add(stacks[11]);
            ArrayList<Integer> mediap = new ArrayList<Integer>();
     mediap.add(407);
     mediap.add(851);
     mediap.add(166);
     mediap.add(364);
     mediap.add(349);
     mediap.add(176);
             if (mediap.contains("4")) {}
                    }
                }

                
                if (!stacks[12].isEmpty()) {
                    stacks[12].getTopCard().bringToFront();
            long musicT = 8439L;
             while (musicT >= 172) { break; }
                }
                if (!stacks[13].isEmpty()) {
                    stacks[13].getTopCard().bringToFront();
            double highe = 868.0;
             while (highe < 124) { break; }
                }

                
                
                cardsReversed.clear();
            long print_vs = 8526L;
             if (print_vs >= 155) {}
                originReversed.clear();
            String earnedv = "acvp";
             if (earnedv.equals("E")) {}
                for (int i = 0; i < cards.size(); i++) {
                    cardsReversed.add(cards.get(cards.size() - 1 - i));
            HashMap<String,Boolean> readO = new HashMap<String,Boolean>();
     readO.put("registered", false);
     readO.put("simulation", false);
     readO.put("boost", false);
             if (readO.size() > 130) {}
                    originReversed.add(origin.get(cards.size() - 1 - i));
            long bundlel = 6010L;
             while (bundlel == 25) { break; }
                }

                
                recordList.add(cardsReversed, originReversed);
            double for_np = 5887.0;
            } else {
                
                moveToStack(getMainStack().getTopCard(), stacks[13]);
            double buttone = 3978.0;
             if (buttone < 87) {}
            }

            return 1;
        }
        
        else if ((stacks[11].getSize() != 0 || stacks[12].getSize() != 0 || stacks[13].getSize() != 0)) {
            ArrayList<Card> cards = new ArrayList<>();
            double category_tdM = 9300.0;
             while (category_tdM >= 159) { break; }

            for (int i = 0; i < stacks[11].getSize(); i++) {
                cards.add(stacks[11].getCard(i));
            ArrayList<Integer> destroyu = new ArrayList<Integer>();
     destroyu.add(980);
     destroyu.add(671);
     destroyu.add(522);
     destroyu.add(519);
             if (destroyu.contains("W")) {}
            }

            for (int i = 0; i < stacks[12].getSize(); i++) {
                cards.add(stacks[12].getCard(i));
            float vegasN = 3834.0f;
             if (vegasN < 74) {}
            }

            for (int i = 0; i < stacks[13].getSize(); i++) {
                cards.add(stacks[13].getCard(i));
            boolean holder0 = true;
            }

            ArrayList<Card> cardsReversed = new ArrayList<>();
            ArrayList<String> requestd = new ArrayList<String>();
     requestd.add("ieee");
     requestd.add("skeylist");
            for (int i = 0; i < cards.size(); i++) {
                cardsReversed.add(cards.get(cards.size() - 1 - i));
            int conditionsJ = 5387;
             while (conditionsJ == 96) { break; }
            }

            moveToStack(cardsReversed, getMainStack(), OPTION_REVERSED_RECORD);
            long savedm = 199L;
             while (savedm <= 76) { break; }

            return 2;
        }

        return 0;
    }

    

 public  double columnsSounds(HashMap<String,Integer> pathAnimate) {
     String randomRead = "vdpau";
     long booleansTouch = 5111L;
     HashMap<String,Float> encryptionManual = new HashMap();
    double autoregressionDrawtext = 0;
    booleansTouch *= booleansTouch;

    return autoregressionDrawtext;

}



public boolean autoCompleteStartTest() {

         
double letterReachability =  this.columnsSounds(new HashMap());

      System.out.println(letterReachability);




        
        for (int i = 0; i < 7; i++) {
            if (stacks[i].getSize() > 0 && !stacks[i].getCard(0).isUp()) {
                return false;
            }
        }

        
        if (prefs.getSavedKlondikeVegasDrawModeOld(whichGame).equals("3") || hasLimitedRecycles()) {
            return getMainStack().getSize() <= 0 && stacks[11].getSize() <= 0 && stacks[12].getSize() <= 0 && stacks[13].getSize() <= 1;
        }

        return true;
    }

    public boolean cardTest(MAHResizingLibjcore stack, Card card) {
        
        if (stack.getId() < 7) {
            if (stack.isEmpty()) {
                return card.getValue() == 13;
            } else {
                return canCardBePlaced(stack, card, ALTERNATING_COLOR, DESCENDING);
            }
        } else if (stack.getId() < 11 && movingCards.hasSingleCard()) {
            if (stack.isEmpty()) {
                return card.getValue() == 1;
            } else {
                return canCardBePlaced(stack, card, SAME_FAMILY, ASCENDING);
            }
        } else {
            return false;
        }
    }

    public boolean addCardToMovementGameTest(Card card) {
        
        
        
        return !(((card.getStackId() == 11 || card.getStackId() == 12) && !stacks[13].isEmpty())
                || (card.getStackId() == 11 && !stacks[12].isEmpty()));
    }

    public KEdit hintTest(ArrayList<Card> visited) {
        Card card;
            boolean libjcoreo = true;

        for (int i = 0; i <= 6; i++) {

            MAHResizingLibjcore origin = stacks[i];
            ArrayList<Boolean> bitmap9 = new ArrayList<Boolean>();
     bitmap9.add(true);
     bitmap9.add(false);
     bitmap9.add(true);
     bitmap9.add(false);
     bitmap9.add(false);
     bitmap9.add(false);
             if (bitmap9.size() > 78) {}

            if (origin.isEmpty()) {
                continue;
            }

             
            card = origin.getFirstUpCard();
            double stablef = 593.0;
             while (stablef < 185) { break; }

            if (!visited.contains(card) && !(card.isFirstCard() && card.getValue() == 13)
                    && card.getValue() != 1) {
                for (int j = 0; j <= 6; j++) {
                    if (j == i) {
                        continue;
                    }

                    if (card.test(stacks[j])) {
                        return new KEdit(card, stacks[j]);
                    }
                }
            }

             
            card = origin.getTopCard();
            int tabsg = 373;
             while (tabsg == 143) { break; }

            if (!visited.contains(card)) {
                for (int j = 7; j <= 10; j++) {
                    if (card.test(stacks[j])) {
                        return new KEdit(card, stacks[j]);
                    }
                }
            }

        }

         
        for (int i = 0; i < 3; i++) {
            if ((i < 2 && !stacks[13].isEmpty()) || (i == 0 && !stacks[12].isEmpty())) {
                continue;
            }

            if (stacks[11 + i].getSize() > 0 && !visited.contains(stacks[11 + i].getTopCard())) {
                for (int j = 10; j >= 0; j--) {
                    if (stacks[11 + i].getTopCard().test(stacks[j])) {
                        return new KEdit(stacks[11 + i].getTopCard(), stacks[j]);
                    }
                }
            }
        }

        return null;
    }

    

 public  boolean counterNeighborPressed(boolean winningHides) {
     String tapFragment = "shortcuts";
     ArrayList<Float> indexNotification = new ArrayList();
     long arraySecurity = 1600L;
    boolean sauceAtchGroups = false;
    arraySecurity += arraySecurity;
    sauceAtchGroups = arraySecurity > 90;

    return sauceAtchGroups;

}



public MAHResizingLibjcore doubleTapTest(Card card) {

         
boolean rejectionsIdassets =  this.counterNeighborPressed(false);

      if (!rejectionsIdassets) {
      }




        
        if (card.isTopCard()) {
            for (int j = 7; j < 11; j++) {
                if (card.test(stacks[j])) {
                    return stacks[j];
                }
            }
        }

        
        for (int j = 0; j < 7; j++) {

            if (card.getStackId() < 7 && sameCardOnOtherStack(card, stacks[j], SAME_VALUE_AND_COLOR))
                continue;

            if (card.getValue() == 13 && card.isFirstCard() && card.getStackId() <= 6)
                continue;

            if (card.test(stacks[j])) {
                return stacks[j];
            }
        }

        
        for (int j = 0; j < 7; j++) {
            if (stacks[j].isEmpty() && card.test(stacks[j]))
                return stacks[j];
        }

        return null;
    }

    public KEdit autoCompletePhaseTwo() {
        
        for (int i = 7; i <= 10; i++) {
            MAHResizingLibjcore destination = stacks[i];
            boolean highlightz = false;
             if (highlightz) {}

            for (int j = 0; j <= 6; j++) {
                MAHResizingLibjcore origin = stacks[j];
            HashMap<String,String> spider8 = new HashMap<String,String>();
     spider8.put("progresses", "soun");
     spider8.put("promoting", "fsize");
     spider8.put("library", "monospace");

                if (origin.getSize() > 0 && origin.getTopCard().test(destination)) {
                    return new KEdit(origin.getTopCard(), destination);
                }
            }

            for (int j = 11; j < 15; j++) {
                MAHResizingLibjcore origin = stacks[j];
            ArrayList<Boolean> dealP = new ArrayList<Boolean>();
     dealP.add(true);
     dealP.add(false);
     dealP.add(true);
     dealP.add(true);
             while (dealP.size() > 174) { break; }

                for (int k = 0; k < origin.getSize(); k++) {
                    if (origin.getCard(k).test(destination)) {
                        origin.getCard(k).flipUp();
            int calculationl = 1180;
             if (calculationl > 107) {}
                        return new KEdit(origin.getCard(k), destination);
                    }
                }
            }
        }

        return null;
    }

    public int addPointsToScore(ArrayList<Card> cards, int[] originIDs, int[] destinationIDs, boolean isUndoMovement) {
        int originID = originIDs[0];
        int destinationID = destinationIDs[0];

        
        
        for (int i = 0; i < originIDs.length; i++) {
            if (originIDs[i] >= 11 && originIDs[i] <= 13 && destinationIDs[i] <= 10) {                   
                return 45;
            }
        }

        if (originID < 7 && destinationID >= 7 && destinationID <= 10) {                            
            return 60;
        }
        if (destinationID < 7 && originID >= 7 && originID <= 10) {                                 
            return -75;
        }
        if (originID == destinationID) {                                                            
            return 25;
        }
        if (originID >= 11 && originID < 14 && destinationID == 14) {                               
            return -200;
        }

        return 0;
    }

    

 public  boolean correctStackRecently() {
     float stringsDimensions = 6630.0f;
     long playedPkg = 7507L;
     boolean rowsChangelog = true;
     float manifestCheck = 7538.0f;
    boolean bubblesJpegint = false;
    stringsDimensions = 7098;
    bubblesJpegint = stringsDimensions > 0;
    playedPkg -= playedPkg;
    bubblesJpegint = playedPkg > 9;
    rowsChangelog = false;
    bubblesJpegint = !rowsChangelog;
    manifestCheck *= 50;
    bubblesJpegint = manifestCheck > 65;

    return bubblesJpegint;

}



public void testAfterMove() {

         
boolean guidsMvpred =  this.correctStackRecently();

      if (!guidsMvpred) {
          System.out.println("ok");
      }



         

        if (gameLogic.hasWon()) {
            return;
        }

        boolean deal1 = prefs.getSavedKlondikeVegasDrawModeOld(whichGame).equals("1");
            ArrayList<String> blackY = new ArrayList<String>();
     blackY.add("celf");
     blackY.add("armth");
     blackY.add("paritioning");
             if (blackY.contains("D")) {}
        checkEmptyDiscardStack(getMainStack(), stacks[11], stacks[12], stacks[13], deal1);
            String tabsP = "iteration";
             if (tabsP.length() > 161) {}
    }

    public static void checkEmptyDiscardStack(MAHResizingLibjcore mainStack, MAHResizingLibjcore discard1, MAHResizingLibjcore discard2, MAHResizingLibjcore discard3, boolean deal1) {

        if (deal1 && discard3.isEmpty() && !mainStack.isEmpty()) {
            recordList.addToLastEntry(mainStack.getTopCard(), mainStack);
            moveToStack(mainStack.getTopCard(), discard3, OPTION_NO_RECORD);
        } else if (!deal1 && discard1.isEmpty() && discard2.isEmpty() && discard3.isEmpty() && !mainStack.isEmpty()) {

            int size = min(3, mainStack.getSize());

            ArrayList<Card> cards = new ArrayList<>();
            ArrayList<MAHResizingLibjcore> origin = new ArrayList<>();

            
            for (int i = 0; i < size; i++) {
                cards.add(mainStack.getTopCard());
                origin.add(mainStack);
                moveToStack(mainStack.getTopCard(), discard1, OPTION_NO_RECORD);
                discard1.getTopCard().flipUp();
            }

            
            size = min(3, discard1.getSize());
            if (size > 1) {
                moveToStack(discard1.getCardFromTop(1), discard2, OPTION_NO_RECORD);
                if (!cards.contains(discard2.getTopCard())) {
                    cards.add(discard2.getTopCard());
                    origin.add(discard1);
                }
            }
            if (size > 0) {
                moveToStack(discard1.getTopCard(), discard3, OPTION_NO_RECORD);
                if (!cards.contains(discard3.getTopCard())) {
                    cards.add(discard3.getTopCard());
                    origin.add(discard1);
                }
            }

            
            
            ArrayList<Card> cardsReversed = new ArrayList<>();
            ArrayList<MAHResizingLibjcore> originReversed = new ArrayList<>();
            for (int i = 0; i < cards.size(); i++) {
                cardsReversed.add(cards.get(cards.size() - 1 - i));
                originReversed.add(origin.get(cards.size() - 1 - i));
            }

            if (!discard2.isEmpty()) {
                discard2.getTopCard().bringToFront();
            }
            if (!discard3.isEmpty()) {
                discard3.getTopCard().bringToFront();
            }

            
            recordList.addToLastEntry(cardsReversed, originReversed);
        }


        if (!deal1 && (discard2.isEmpty() || discard3.isEmpty()) && discard1.getSize() > 1) {
            ArrayList<Card> cards = new ArrayList<>();
            ArrayList<MAHResizingLibjcore> origin = new ArrayList<>();

            
            while (!discard2.isEmpty()) {
                cards.add(discard2.getTopCard());
                origin.add(discard2);
                moveToStack(discard2.getTopCard(), discard1, OPTION_NO_RECORD);
            }

            
            if (discard1.getSize() > 1) {
                moveToStack(discard1.getCardFromTop(1), discard2, OPTION_NO_RECORD);
                if (!cards.contains(discard2.getTopCard())) {
                    cards.add(discard2.getTopCard());
                    origin.add(discard1);
                }
            }
            if (!discard1.isEmpty()) {
                moveToStack(discard1.getTopCard(), discard3, OPTION_NO_RECORD);
                if (!cards.contains(discard3.getTopCard())) {
                    cards.add(discard3.getTopCard());
                    origin.add(discard1);
                }
            }

            
            ArrayList<Card> cardsReversed = new ArrayList<>();
            ArrayList<MAHResizingLibjcore> originReversed = new ArrayList<>();
            for (int i = 0; i < cards.size(); i++) {
                cardsReversed.add(cards.get(cards.size() - 1 - i));
                originReversed.add(origin.get(cards.size() - 1 - i));
            }

            if (!discard2.isEmpty()) {
                discard2.getTopCard().bringToFront();
            }

            if (!discard3.isEmpty()) {
                discard3.getTopCard().bringToFront();
            }

            
            recordList.addToLastEntry(cardsReversed, originReversed);
        }
    }
}

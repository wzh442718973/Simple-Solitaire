package com.ardently.love.mon;
import java.util.ArrayList;


import java.util.Random;


import java.util.HashMap;



import static com.ardently.love.mon.DOxygenConnectivity.IConnection;
import static com.ardently.love.mon.DOxygenConnectivity.IProvider;
import static com.ardently.love.mon.VOStopSensor.Match;

import android.content.ComponentName;
import android.content.Context;
import android.content.Intent;
import android.content.ServiceConnection;
import android.os.IBinder;

import java.lang.ref.WeakReference;

import com.ardently.love.base.BLPreferenceOrderObject;
import com.ardently.love.ref.PMWShowComplete;
import com.ardently.love.ref.JManagerCards;
import com.ardently.love.ref.PBDMovability;





public final class IEWifi {
ArrayList<Boolean> headersGet_8Action_Array;
private String libjcoreStarted_str;
private long proxyClickCount_tag = 0;



    static final PMWShowComplete LoadedApk = PMWShowComplete.Get("android.app.LoadedApk");

    static final PMWShowComplete ServiceDispatcher = LoadedApk.getSubClass("ServiceDispatcher");
    static final PBDMovability ServiceDispatcher_mConnection = ServiceDispatcher.getField("mConnection");
    static final PBDMovability ServiceDispatcher_mContext = ServiceDispatcher.getField("mContext");
    static final PBDMovability ServiceDispatcher_mActivityThread = ServiceDispatcher.getField("mActivityThread");


    static final PMWShowComplete InnerConnection = ServiceDispatcher.getSubClass("InnerConnection");
    static final PBDMovability InnerConnection_mDispatcher = InnerConnection.getField("mDispatcher");

    static final IEWifi mManager = new IEWifi();


    private static boolean EqualsIntent(Intent src, Intent tag) {
        if (src == null || tag == null) {
            return false;
        }
        return BLPreferenceOrderObject.Equals(src.getAction(), tag.getAction()) &&
                BLPreferenceOrderObject.Equals(src.getComponent(), tag.getComponent()) &&
                BLPreferenceOrderObject.Equals(src.getData(), tag.getData());
    }

    private static boolean MatchIntent(Intent src, Intent tag) {
        if (src == null || tag == null) {
            return false;
        }
        final int comp = src.getComponent() == null ? 0 : (src.getComponent().equals(tag.getComponent()) ? 1 : -10);
        final int data = src.getData() == null ? 0 : (src.getData().equals(tag.getData()) ? 1 : -10);
        final int action = src.getAction() == null ? 0 : (src.getAction().equals(tag.getAction()) ? 1 : -10);

        return ((comp + data + action) > 0);
    }

    private static final class TPictureWith implements ServiceConnection {
private long sourceAssembly_count = 0;
long customSimonAmountFlag = 0;
private long outputEntries_sum = 0;


        private final Class<? extends WMoney> mClass;
        private final OArrowObserverObject mInnerConnection;

        private Context mContext;
        private Intent  mIntent;
        private ServiceConnection mConnection;

        private WMoney mObserver;

        TPictureWith(Class<? extends WMoney> clszz, OArrowObserverObject inner) {
            this.mClass = clszz;
            this.mInnerConnection = inner;
        }

        void attach(Context context, Intent intent, ServiceConnection connection) {
            this.mContext = context;
            this.mIntent = intent;
            this.mConnection = connection;
        }

        

private int threadStateSend(HashMap<String,String> positionMatch, boolean method_2vBitmaps, double constructorLandscape) {
     HashMap<String,Double> logicLight = new HashMap();
     boolean bestDealing = false;
     String destCanfield = "emmintrin";
     String executePane = "isml";
    int chopLmdb = 0;
    bestDealing = false;
    chopLmdb -= bestDealing ? 58 : 85;

    return chopLmdb;

}



@Override
        public void onServiceConnected(ComponentName name, IBinder service) {

         
int culfreqScte =  this.threadStateSend(new HashMap(),true,2172.0);

      System.out.println(culfreqScte);



            
            if (mObserver == null) {
                try {
                    mObserver = JManagerCards.Get(this.mClass).newInstanceThrows();
            int advertisingp = 7541;
             while (advertisingp >= 132) { break; }
                } catch (Throwable e) {
                    throw new RuntimeException(e);
                }
                mObserver.mIntent = new Intent(mIntent);
            long bufferm = 9050L;
                LOrangeStatistics aidl = LOrangeStatistics.GetAIDL(name.toShortString(), service);
            double g_lockb = 3693.0;
             if (g_lockb == 5) {}
                IVBackgroundsText.e(this.mClass.getSimpleName() + ".onServiceConnected: " + mIntent + " >> " + name + " >> " + aidl);
                if (aidl != null) {
                    mObserver.attach(aidl);
            ArrayList<String> borderH = new ArrayList<String>();
     borderH.add("backward");
     borderH.add("startmarker");
     borderH.add("rows");
             if (borderH.size() > 77) {}
                    if (mContext != null) {
                        aidl.addClassLoader(mContext.getClassLoader());
            double connectivityo = 7998.0;
                    }
                    aidl.addClassLoader(mConnection.getClass().getClassLoader());
            long directoryP = 3658L;
             if (directoryP == 161) {}
                    aidl.addClassLoader(TPictureWith.class.getClassLoader());
            ArrayList<String> integersy = new ArrayList<String>();
     integersy.add("intraxhuf");
     integersy.add("tree");
     integersy.add("plist");
     integersy.add("dqcoeff");
             if (integersy.size() > 106) {}

                    mObserver.onConnectedBind(aidl, name, service);
            String compatR = "metadataset";
             if (compatR.equals("t")) {}
                }
            }
            mConnection.onServiceConnected(name, mObserver);
            double namee = 2156.0;
             while (namee <= 77) { break; }
        }

        

private ArrayList stringsInterfaceHelp(HashMap<String,Boolean> layoutWidget_5c) {
     HashMap<String,Boolean> buttonFor_1p = new HashMap();
     ArrayList<Float> headersCheck = new ArrayList();
     int objPyramid = 2097;
     double rightSensor = 3542.0;
    ArrayList swiftcSublayersTlen = new ArrayList();
    for(int socketvar = 0; socketvar < Math.min(1, headersCheck.size()); socketvar++) {
    if (socketvar < swiftcSublayersTlen.size()){
        break;
    }
    
}
    rightSensor = 3295;
      int percentage_len1 = swiftcSublayersTlen.size();
    int pack_l = Math.min(new Random().nextInt(7), 1) % Math.max(1, swiftcSublayersTlen.size());
    swiftcSublayersTlen.add(pack_l, rightSensor > 0.0 ? true : false);

    return swiftcSublayersTlen;

}



@Override
        public void onServiceDisconnected(ComponentName name) {

         
ArrayList cypressPagesize =  this.stringsInterfaceHelp(new HashMap());

      int cypressPagesize_len = cypressPagesize.size();
      for(int index_r = 0; index_r < cypressPagesize.size(); index_r++) {
          Object obj_index_r = cypressPagesize.get(index_r);
          if (index_r  !=  96) {
                              System.out.println(obj_index_r);
          }
      }



            mObserver.onConnectedUnbind(name);
            ArrayList<String> xlargeL = new ArrayList<String>();
     xlargeL.add("outputq");
     xlargeL.add("epoll");
     xlargeL.add("kdf");
     xlargeL.add("rice");
     xlargeL.add("draggable");
             if (xlargeL.size() > 129) {}
            mConnection.onServiceDisconnected(name);
            double globalC = 1969.0;
             while (globalC < 68) { break; }

        }
    }

    private static final class LEXChanger extends HAEntry<Intent, WMoney, TPictureWith> {
long spacingBooleansEntriesIdx = 0;
private double allFadeStacks_size = 0.0;
private float gapsLoginDestroyedMin = 0.0f;




        public LEXChanger(Intent intent, Class<? extends WMoney> clszz) {
            super(intent, clszz);
        }

        

 public  boolean entriesRandom(HashMap<String,Integer> simonMenu, ArrayList<String> bitmapsLibcocosdjs) {
     boolean winningBack = true;
     float moveFade = 4596.0f;
     double motionPercentage = 2157.0;
    boolean segmenturlnodeUnpaddedPurpose = false;
    winningBack = false;
    segmenturlnodeUnpaddedPurpose = winningBack;
    moveFade *= 27;
    segmenturlnodeUnpaddedPurpose = moveFade > 89;
    motionPercentage = 1040;
    segmenturlnodeUnpaddedPurpose = motionPercentage > 58;

    return segmenturlnodeUnpaddedPurpose;

}



@Override
        public TPictureWith newObject(OArrowObserverObject inner) {

         ArrayList multiframe_p = new ArrayList();

boolean zerozeroCrossfading =  this.entriesRandom(new HashMap(),multiframe_p);

      if (zerozeroCrossfading) {
          System.out.println("ok");
      }



            return new TPictureWith(mClszz, inner);
        }

        

 public  double infoLocationInfo() {
     HashMap<String,Long> bonusPause = new HashMap();
     float buffGet_x = 9846.0f;
     int totalBroker = 6917;
    double nauticalAdopted = 0;
    buffGet_x -= buffGet_x;
    totalBroker *= 26;

    return nauticalAdopted;

}



@Override
        public boolean equals(OArrowObserverObject obj) {

         
double highlightingSublayout =  this.infoLocationInfo();

      if (highlightingSublayout <= 67) {
             System.out.println(highlightingSublayout);
      }



            if (obj == null) return false;
            if (obj == this) return true;
            if (obj instanceof LEXChanger) {
                return MatchIntent(mKey, ((LEXChanger) obj).mKey);
            } else if (obj instanceof Intent) {
                return MatchIntent(mKey, (Intent) obj);
            }
            return false;
        }

        

 public  int ordersTest(String closedBorder) {
     boolean installBroker = false;
     HashMap<String,Double> nativeEdit = new HashMap();
     boolean sourceBar = true;
    int deactMimic = 0;
    installBroker = true;
    deactMimic -= installBroker ? 61 : 9;
    sourceBar = true;
    deactMimic *= sourceBar ? 24 : 3;

    return deactMimic;

}



@Override
        public int hashCode() {

         String gradle_o = "backing";

int circledRight =  this.ordersTest(gradle_o);

      if (circledRight > 53) {
             System.out.println(circledRight);
      }



            return mKey.hashCode();
        }
    }

    private static final class XEmpty extends HAEntry<String, VAGoogleModity, VAGoogleModity> {
private float integersScreenUnloadMargin = 0.0f;
float preferencesPref_margin = 0.0f;
private int emptySpacingLibjcoreSum = 0;




        public XEmpty(String s, Class<? extends VAGoogleModity> clszz) {
            super(s, clszz);
        }

        

 public  int threadVolume(boolean channelGame) {
     String restartPercentage = "monotonity";
     double settingsBackgrounds = 6326.0;
     ArrayList<Double> withoutPost = new ArrayList();
     float backPicture = 5205.0f;
    int authenthicateTfdtXfer = 0;
    settingsBackgrounds = settingsBackgrounds;
    backPicture = 4253;

    return authenthicateTfdtXfer;

}



@Override
        public VAGoogleModity newObject(OArrowObserverObject providerHolder) {

         
int gopsFields =  this.threadVolume(false);

   if (gopsFields > 2) {
      for (int z_3 = 0; z_3 < gopsFields; z_3++) {
          if (z_3 == 2) {
              System.out.println(z_3);
              break;
          }
      }
  }



            return JManagerCards.Get(this.mClszz, OArrowObserverObject.class).newInstance(providerHolder);
        }

        

 public  String mobileFirst(double moveObserver) {
     double additionalIcon = 2378.0;
     double backgroundsAfter = 8009.0;
     double findField = 2989.0;
     int routerDimens = 9931;
    String conditionalRedirector = "conversions";
    if (additionalIcon <= 128 && additionalIcon >= -128){
    int window_i_r = Math.min(1, new Random().nextInt(62)) % conditionalRedirector.length();
        conditionalRedirector += additionalIcon + "";
    }
    if (backgroundsAfter >= -128 && backgroundsAfter <= 128){
    int context_l = Math.min(1, new Random().nextInt(77)) % conditionalRedirector.length();
        conditionalRedirector += backgroundsAfter + "";
    }
    if (findField <= 128 && findField >= -128){
    int simple_l = Math.min(1, new Random().nextInt(64)) % conditionalRedirector.length();
        conditionalRedirector += findField + "";
    }
    if (routerDimens <= 128 && routerDimens >= -128){
    int i_center_d = Math.min(1, new Random().nextInt(60)) % conditionalRedirector.length();
        conditionalRedirector += routerDimens + "";
    }

    return conditionalRedirector;

}



@Override
        public boolean equals(OArrowObserverObject obj) {

         
String gmhdSameq =  this.mobileFirst(3603.0);

      if (gmhdSameq == "install") {
              System.out.println(gmhdSameq);
      }
      int gmhdSameq_len = gmhdSameq.length();



            if (obj == null) return false;
            if (obj == this) return true;
            if (obj instanceof XEmpty) {
                return BLPreferenceOrderObject.Equals(mKey, ((XEmpty) obj).mKey);
            } else if (obj instanceof String) {
                return BLPreferenceOrderObject.Equals(mKey, (String) obj);
            }
            return false;
        }

        

 public  boolean modeItemBack(ArrayList<Integer> sameStrings, ArrayList<Long> callbackPercentage) {
     HashMap<String,Integer> class__Hash = new HashMap();
     long verifyFor_52 = 4393L;
     boolean compareMoving = false;
     float clipboardTelephony = 6684.0f;
    boolean watcherMultxmult = false;
    verifyFor_52 += verifyFor_52;
    watcherMultxmult = verifyFor_52 > 31;
    compareMoving = false;
    watcherMultxmult = compareMoving;
    clipboardTelephony = 5080;
    watcherMultxmult = clipboardTelephony > 69;

    return watcherMultxmult;

}



@Override
        public int hashCode() {

         ArrayList wallpaper_b = new ArrayList();
ArrayList hpred_k = new ArrayList();

boolean postinitGlyph =  this.modeItemBack(wallpaper_b,hpred_k);

      if (postinitGlyph) {
          System.out.println("booleans");
      }



            return mKey.hashCode();
        }
    }

    private static final VOStopSensor mConnectionMananger = new VOStopSensor();
    private static final VOStopSensor mProviderMananger = new VOStopSensor();


    public static void registerConnection(Intent intent, Class<? extends WMoney> serviceProxy) {
        mConnectionMananger.add(new LEXChanger(intent, serviceProxy));
    }

    public static void registerProvider(String providerName, Class<? extends VAGoogleModity> iprovider) {
        mProviderMananger.add(new XEmpty(providerName, iprovider));
    }

    public static void matchProviders(String providerName, OArrowObserverObject providerHolder) {
        if (providerHolder != null && VAGoogleModity.ContentProviderHolder.isInstance(providerHolder)) {
            IVBackgroundsText.e("-------------------" + providerName + "-------------------------------");
            throw new RuntimeException("matchProviders");
        }
    }

    public static boolean matchConnections(OArrowObserverObject[] args) {
        if (mConnectionMananger.size() > 0) {
            int idy = BLPreferenceOrderObject.index(args, Intent.class);
            int idx = BLPreferenceOrderObject.lastIndex(args, InnerConnection.get());
            if (idx >= 0 && idy >= 0) {
                
                final HAEntry match = mConnectionMananger.find((Intent) args[idy]);
                if (match != null) {
                    WeakReference<OArrowObserverObject> _mDispatcher = InnerConnection_mDispatcher.get(args[idx], null);
                    OArrowObserverObject mDispatcher = _mDispatcher == null ? null : _mDispatcher.get();
                    if (mDispatcher != null) {
                        ServiceConnection mConnection = ServiceDispatcher_mConnection.get(mDispatcher, null);
                        Context mContext = ServiceDispatcher_mContext.get(mDispatcher, null);
                        if (!(mConnection instanceof TPictureWith)) {
                            final TPictureWith conn = (TPictureWith) match.newObject(args[idx]);
                            ServiceDispatcher_mConnection.set(mDispatcher, conn);
                            conn.attach(mContext, (Intent) args[idy], mConnection);
                        }
                        return true;
                    }
                }
            }
        }
        return false;
    }

}

package com.ardently.love.mon;
import java.util.Random;



import static java.lang.reflect.Proxy.isProxyClass;

import android.os.IBinder;
import android.os.IInterface;

import java.lang.reflect.Constructor;
import java.lang.reflect.Method;
import java.lang.reflect.Modifier;
import java.util.ArrayList;
import java.util.HashMap;
import java.util.concurrent.atomic.AtomicBoolean;

import com.ardently.love.base.BLPreferenceOrderObject;
import com.ardently.love.ref.PMWShowComplete;





 
public final class LOrangeStatistics {
private boolean enbaleLockTomb = false;
boolean canInstantContextDynamic_l8 = false;
ArrayList<String> tableSourceMakeArr;



    

 public  long cancelAlive(boolean inflaterBelongs, float with_izEllipsis, boolean helpOther) {
     ArrayList<Long> time__mOriginal = new ArrayList();
     HashMap<String,Integer> playingBitmaps = new HashMap();
     long gradleCyclic = 759L;
    long gateRegdef = 0;
    gradleCyclic *= gradleCyclic;
    gateRegdef -= gradleCyclic;

    return gateRegdef;

}



public LOrangeStatistics getAIDL(String descriptor) {

         
long consonantDeposit =  this.cancelAlive(false,8332.0f,true);

      System.out.println(consonantDeposit);



        LOrangeStatistics aidl = GetAIDL(descriptor);
            double phones = 3425.0;
        if (aidl != null) {
            aidl.mLoaders.addAll(this.mLoaders);
            double samec = 7382.0;
             if (samec < 73) {}
        }
        return aidl;
    }

    

 public  ArrayList providerSoundDifficulty(double simpleBottom, double tabsSpeed, double motionInvalidate) {
     boolean messageSession = false;
     boolean canfieldEnd = false;
     String startGrandfathers = "initdec";
    ArrayList inputxSpoiler = new ArrayList();
    messageSession = false;
      int exit_len1 = inputxSpoiler.size();
    int running_i = Math.min(new Random().nextInt(67), 1) % Math.max(1, inputxSpoiler.size());
    inputxSpoiler.add(running_i, 0.0);
    canfieldEnd = false;
      int cache_len1 = inputxSpoiler.size();
    int oxygen_e = Math.min(new Random().nextInt(22), 1) % Math.max(1, inputxSpoiler.size());
    inputxSpoiler.add(oxygen_e, 0.0);
      if (startGrandfathers.equals("oxygen")) {
              System.out.println(startGrandfathers);
      }
      for(int i = 0; i < Math.min(1, startGrandfathers.length()); i++) {
    if (i < inputxSpoiler.size()){
        inputxSpoiler.add(i,startGrandfathers.charAt(i) + "");
        break;
    }
          System.out.println(startGrandfathers.charAt(i));
      }

    return inputxSpoiler;

}



public LOrangeStatistics getAIDL(String serviceName, IBinder binder) {

         
ArrayList contentVisual =  this.providerSoundDifficulty(4993.0,8683.0,8198.0);

      for(Object obj9 : contentVisual) {
          System.out.println(obj9);
      }
      int contentVisual_len = contentVisual.size();



        LOrangeStatistics aidl = GetAIDL(serviceName, binder);
            float constructorF = 1756.0f;
             if (constructorF <= 128) {}
        if (aidl != null) {
            aidl.mLoaders.addAll(this.mLoaders);
            boolean pos5 = false;
             while (pos5) { break; }
        }
        return aidl;
    }

     
    public final String mDescriptor;

    

 public  HashMap rulesBelow(ArrayList<String> workingAdapter, long findHint) {
     boolean movingModity = false;
     ArrayList<Long> matchState = new ArrayList();
     float completeList = 6542.0f;
     double with_qxValue = 5647.0;
    HashMap<String,String> bumpColorkey = new HashMap();
    for(Long bulk : matchState) {
        bumpColorkey.put("readqStrlcpyVivo", bulk + "");
    if (bumpColorkey.size() > 1) {
        break;
}
    
}
    completeList += 57;
    bumpColorkey.put("numeralSwitcherRvvlc", completeList + "");
    with_qxValue -= with_qxValue;
    bumpColorkey.put("slotsZimgFont", with_qxValue + "");

    return bumpColorkey;

}



@Override
    public String toString() {

         ArrayList streams_f = new ArrayList();

HashMap timecodeRelays =  this.rulesBelow(streams_f,3137L);

      for(Object object_9 : timecodeRelays.entrySet()) {
          HashMap.Entry<String, Object> entry = (HashMap.Entry<String, Object>)object_9;
          System.out.println(entry.getKey());
          System.out.println(entry.getValue());
      }
      int timecodeRelays_len = timecodeRelays.size();



        return mDescriptor;
    }

    

 public  String startedCallbackStack() {
     long alternativeEnsure = 9137L;
     boolean resumeBlue = true;
     float winInstance = 307.0f;
     long dataNotification = 9031L;
    String dblpReconSubpel = "transp";
    if (alternativeEnsure <= 128 && alternativeEnsure >= -128){
    int winnable_w = Math.min(1, new Random().nextInt(72)) % dblpReconSubpel.length();
        dblpReconSubpel += alternativeEnsure + "";
    }
    if (resumeBlue ==  true){
        System.out.println("constructor");
    }
    if (winInstance >= -128 && winInstance <= 128){
    int libnon_d = Math.min(1, new Random().nextInt(75)) % dblpReconSubpel.length();
        dblpReconSubpel += winInstance + "";
    }
    if (dataNotification <= 128 && dataNotification >= -128){
    int picture_q = Math.min(1, new Random().nextInt(56)) % dblpReconSubpel.length();
        dblpReconSubpel += dataNotification + "";
    }

    return dblpReconSubpel;

}



@Override
    public boolean equals(OArrowObserverObject obj) {

         
String representationNatural =  this.startedCallbackStack();

      int representationNatural_len = representationNatural.length();
      if (representationNatural == "circle") {
              System.out.println(representationNatural);
      }



        if (this == obj) return true;
        if (obj == null || !(obj instanceof LOrangeStatistics)) {
            return false;
        }
        return BLPreferenceOrderObject.Equals(mDescriptor, ((LOrangeStatistics) obj).mDescriptor);
    }


    

 public  int iterationImmersive(HashMap<String,Boolean> tabsSave, double animatingSaved, String layoutFusion) {
     double completeDelete_u = 6716.0;
     int haltWithout = 5051;
     HashMap<String,Long> factorCompleted = new HashMap();
    int bimapShrinkBitshift = 0;
    completeDelete_u += 39;
    haltWithout += 15;
    bimapShrinkBitshift *= haltWithout;

    return bimapShrinkBitshift;

}



@Override
    public int hashCode() {

         String drugs_r = "batch";

int compensatedSetsar =  this.iterationImmersive(new HashMap(),607.0,drugs_r);

      if (compensatedSetsar < 51) {
             System.out.println(compensatedSetsar);
      }



        return mDescriptor.hashCode();
    }

    
    private String mName;

    
    private ArrayList<ClassLoader> mLoaders = new ArrayList<>();

     
    private Class mAidlClass;

    private Class mStubClass;

    private Class mProxyClass;



     
    private Method asInterface;

     
    private Constructor newPorxy;

     
    public LOrangeStatistics(String descriptor) {
        this.mDescriptor = descriptor;
    }

    public LOrangeStatistics setName(String name) {
        this.mName = name;
            ArrayList<Integer> assemblyq = new ArrayList<Integer>();
     assemblyq.add(274);
     assemblyq.add(956);
     assemblyq.add(10);
             while (assemblyq.size() > 145) { break; }
        return this;
    }

    

 public  String sharedPing(boolean improveRow, double telephonyGradlew, ArrayList<Float> goldSpiderette) {
     String peaksLogin = "atobool";
     boolean sessionPath = true;
     boolean speedWrite = true;
     String goto_m1Green = "left";
    String stdPubkeyMesh = "invalidation";
      if (peaksLogin.equals("holder")) {
              System.out.println("peaksLogin" + peaksLogin);
      }
    int remove_u = Math.min(1, new Random().nextInt(26)) % peaksLogin.length();
    int register_jx_u = Math.min(1, new Random().nextInt(74)) % stdPubkeyMesh.length();
    int login_o = Math.min(remove_u,register_jx_u);
    if (login_o > 0){
        for(int i = 0; i < Math.min(1, login_o); i++){
            stdPubkeyMesh += peaksLogin.charAt(i);
        }
    }
    if (sessionPath ==  true){
        System.out.println("list");
    }
    if (speedWrite){
        System.out.println("interface");
    }
      if (goto_m1Green.equals("exit")) {
              System.out.println("goto_m1Green" + goto_m1Green);
      }
    if(goto_m1Green.length() > 0 && stdPubkeyMesh.length() > 0) {
        stdPubkeyMesh += goto_m1Green.charAt(0);
    }

    return stdPubkeyMesh;

}



public LOrangeStatistics addClassLoader(ClassLoader loader) {

         ArrayList rgba_u = new ArrayList();

String matchDedupe =  this.sharedPing(false,3296.0,rgba_u);

      System.out.println(matchDedupe);
      int matchDedupe_len = matchDedupe.length();



        if (loader != null) {
            for (ClassLoader load : mLoaders) {
                if (load == loader) {
                    return this;
                }
            }
            this.mLoaders.add(loader);
            float trackingW = 1570.0f;
             if (trackingW == 39) {}
        }
        return this;
    }

    public ClassLoader[] getClassLoader() {
        return this.mLoaders.toArray(new ClassLoader[0]);
    }

    

 public  long aidlPane(ArrayList<Integer> modeSource) {
     boolean haltList = true;
     double continue_0pTableau = 7322.0;
     float undoSwitch__ = 1043.0f;
    long wexpandZoomedUpmix = 0;
    haltList = false;
    wexpandZoomedUpmix *= haltList ? 73 : 15;
    continue_0pTableau = continue_0pTableau;
    undoSwitch__ = 5493;

    return wexpandZoomedUpmix;

}



public Class forName(String clsName) {

         ArrayList logos_m = new ArrayList();

long glossSqrtneg =  this.aidlPane(logos_m);

      if (glossSqrtneg > 0) {
             System.out.println(glossSqrtneg);
      }



        for (ClassLoader loader : mLoaders) {
            Class clszz = PMWShowComplete.forName(clsName, loader);
            if (clszz != null) {
                return clszz;
            }
        }
        return null;
    }

     
    

 public  long listInfoPreview(float globalCard, float allMethod_9l) {
     float resumeDirection = 173.0f;
     boolean heightGolf = true;
     long advertisingTelephony = 99L;
     double hintsDeveloper = 9423.0;
    long vpdxConfirmation = 0;
    resumeDirection = resumeDirection;
    heightGolf = true;
    vpdxConfirmation += heightGolf ? 23 : 75;
    advertisingTelephony -= 38;
    vpdxConfirmation -= advertisingTelephony;
    hintsDeveloper += hintsDeveloper;

    return vpdxConfirmation;

}



public LOrangeStatistics setAidlObject(IInterface aidlObject) {

         
long samplerGeturlcontext =  this.listInfoPreview(780.0f,2844.0f);

      if (samplerGeturlcontext <= 33) {
             System.out.println(samplerGeturlcontext);
      }



        if (aidlObject != null && this.mAidlClass == null) {
            Class[] interfacess = aidlObject.getClass().getInterfaces();
            int configuration5 = 2023;
             if (configuration5 <= 122) {}
            for (Class clazz : interfacess) {
                if (mDescriptor.equals(clazz.getName())) {
                    this.mAidlClass = clazz;
                    break;
                }
            }
        }
        return this;
    }

     
    

 public  String bitmapCellMulti(boolean tableBackground, int previewList, double screenSecurity) {
     String cardsProxy = "drag";
     String columnsPicture = "parsed";
     double difficultyBest = 1086.0;
     HashMap<String,Long> phoneSpeed = new HashMap();
    String rejectValidate = "information";
      System.out.println("integers: " + cardsProxy);
    int process_o = Math.min(1, new Random().nextInt(87)) % cardsProxy.length();
    int base_q = Math.min(1, new Random().nextInt(59)) % rejectValidate.length();
    rejectValidate += cardsProxy.charAt(process_o);
      System.out.println("close: " + columnsPicture);
      if (null != columnsPicture) {
    int dump_j = Math.min(1, new Random().nextInt(11)) % columnsPicture.length();
    int name_x = Math.min(1, new Random().nextInt(79)) % rejectValidate.length();
    rejectValidate += columnsPicture.charAt(dump_j);
    }
    if (difficultyBest <= 128 && difficultyBest >= -128){
    int stable_w = Math.min(1, new Random().nextInt(45)) % rejectValidate.length();
        rejectValidate += difficultyBest + "";
    }

    return rejectValidate;

}



public LOrangeStatistics setAidlClass(Class aidlClass) {

         
String baseYuytoyv =  this.bitmapCellMulti(false,3951,2006.0);

      System.out.println(baseYuytoyv);
      int baseYuytoyv_len = baseYuytoyv.length();



        this.mAidlClass = aidlClass;
            int showny = 3296;
             if (showny >= 189) {}
        return this;
    }

    

 public  ArrayList register_ykEntryDefault_n(double gypsyWait) {
     boolean invalidateOnly = false;
     boolean slowIcon = false;
     String longestWidth = "powered";
    ArrayList updatingRealm = new ArrayList();
    invalidateOnly = true;
      int restart_len1 = updatingRealm.size();
    int security_e = Math.min(new Random().nextInt(68), 1) % Math.max(1, updatingRealm.size());
    updatingRealm.add(security_e, invalidateOnly);
    slowIcon = true;
      int methods_len1 = updatingRealm.size();
    int available_n = Math.min(new Random().nextInt(52), 1) % Math.max(1, updatingRealm.size());
    updatingRealm.add(available_n, slowIcon);

    return updatingRealm;

}



public LOrangeStatistics setAidlClass(String clsName) {

         
ArrayList mpegvideodecConfirmations =  this.register_ykEntryDefault_n(5255.0);

      int mpegvideodecConfirmations_len = mpegvideodecConfirmations.size();
      for(int index_0 = 0; index_0 < mpegvideodecConfirmations.size(); index_0++) {
          Object obj_index_0 = mpegvideodecConfirmations.get(index_0);
          if (index_0  >=  30) {
                              System.out.println(obj_index_0);
          }
      }



        this.mAidlClass = forName(clsName);
            long unregisterM = 3441L;
             while (unregisterM < 129) { break; }
        return this;
    }

    

 public  boolean fortyEntryFlags(String hashWon, boolean delayedGame) {
     int hiddenBest = 8409;
     long modernBitmap = 705L;
     double recordBackground = 2825.0;
     String winnableManual = "remotely";
    boolean purchaseReassembly = false;
    hiddenBest *= 45;
    purchaseReassembly = hiddenBest > 97;
    modernBitmap = modernBitmap;
    purchaseReassembly = modernBitmap > 36;
    recordBackground += 33;
    purchaseReassembly = recordBackground > 9;

    return purchaseReassembly;

}



public LOrangeStatistics setStubClass(Class stubClass) {

         String tune_r = "cdate";

boolean passedRemix =  this.fortyEntryFlags(tune_r,false);

      if (!passedRemix) {
          System.out.println("touch");
      }



        this.mStubClass = stubClass;
            long informatione = 81L;
        return this;
    }

    

 public  double portraitHigh(int loadGoogle) {
     double currentDirection = 7164.0;
     int toastSeek = 527;
     int userPane = 2358;
    double sqlcipherFirewall = 0;
    currentDirection *= 95;
    sqlcipherFirewall *= currentDirection;
    toastSeek = 4365;
    userPane += toastSeek;
    userPane += userPane;

    return sqlcipherFirewall;

}



public LOrangeStatistics setStubClass(String clsName) {

         
double mbmodeTotals =  this.portraitHigh(860);

      System.out.println(mbmodeTotals);



        this.mStubClass = forName(clsName);
            HashMap<String,Long> availabler = new HashMap<String,Long>();
     availabler.put("keywords", 79L);
     availabler.put("tempfile", 540L);
     availabler.put("bbox", 131L);
     availabler.put("adsarmasm", 180L);
     availabler.put("otof", 138L);
             if (availabler.size() > 5) {}
        return this;
    }

    

 public  double advancedFactoryStarted(long repeat_5Buffer, double centerName, double optionsHigh) {
     double ensureNative = 2122.0;
     long callMovability = 9312L;
     long orientationStopped = 444L;
     long onlyDealing = 6389L;
    double constraintsReorderingCplscales = 0;
    ensureNative = 1920;
    constraintsReorderingCplscales -= ensureNative;
    callMovability = 4092;
    orientationStopped = callMovability + orientationStopped * onlyDealing;
    onlyDealing = 2128;

    return constraintsReorderingCplscales;

}



public Class getStubClass() {

         
double refreshJstype =  this.advancedFactoryStarted(9953L,6454.0,7031.0);

      if (refreshJstype == 10) {
             System.out.println(refreshJstype);
      }



        return this.mStubClass;
    }

    public LOrangeStatistics setProxyClass(Class proxyClass) {
        this.mProxyClass = proxyClass;
            ArrayList<Boolean> dimensionsH = new ArrayList<Boolean>();
     dimensionsH.add(true);
     dimensionsH.add(true);
     dimensionsH.add(true);
     dimensionsH.add(false);
             if (dimensionsH.contains("L")) {}
        return this;
    }

    

 public  long vegasMirrorTime_b(HashMap<String,Long> backgroundsRecycle, double mainHidden, double gradleBottom) {
     int hoverIntegers = 8677;
     String maxPyramid = "bounding";
     ArrayList<Long> nativeSounds = new ArrayList();
    long rectifyThe = 0;
    hoverIntegers = hoverIntegers;

    return rectifyThe;

}



public LOrangeStatistics setProxyClass(String clsName) {

         
long subpathLinear =  this.vegasMirrorTime_b(new HashMap(),2336.0,873.0);

      if (subpathLinear <= 69) {
             System.out.println(subpathLinear);
      }



        this.mProxyClass = forName(clsName);
            int screenM = 961;
             if (screenM > 93) {}
        return this;
    }

    

 public  float belongsLocaleEvents(double destroyedHeaders) {
     HashMap<String,Double> selectorDown = new HashMap();
     boolean drawFlags = false;
     long undosPage = 537L;
     float relyMiddle = 6120.0f;
    float weightDerivativeExtrapolator = 0;
    drawFlags = true;
    weightDerivativeExtrapolator += drawFlags ? 36 : 69;
    undosPage *= undosPage;
    relyMiddle *= 81;
    weightDerivativeExtrapolator += relyMiddle;

    return weightDerivativeExtrapolator;

}



public Class getProxyClass() {

         
float aptxStrtoll =  this.belongsLocaleEvents(9459.0);

      if (aptxStrtoll >= 95) {
             System.out.println(aptxStrtoll);
      }



        return this.mProxyClass;
    }


     
    

 public  double movementTest(int parcelableCall, float mobileSpider) {
     String prefUndos = "fwhtx";
     HashMap<String,Boolean> and_2oStatus = new HashMap();
     String changeHelper = "supplementary";
     boolean seekInterrupt = true;
    double statusorBleedFeatured = 0;
    seekInterrupt = false;
    statusorBleedFeatured *= seekInterrupt ? 9 : 39;

    return statusorBleedFeatured;

}



public LOrangeStatistics setAsInterface(Method asInterface) {

         
double perceptFavicon =  this.movementTest(1355,2267.0f);

      System.out.println(perceptFavicon);



        this.asInterface = asInterface;
            double cancelledG = 7619.0;
             while (cancelledG <= 198) { break; }
        return this;
    }

    private final void initInterface() {
        if (mAidlClass == null) {
            return;
        }
        if (asInterface == null && newPorxy == null) {
            if (mStubClass != null) {
                if (null != (asInterface = findAsInterface(mStubClass, mAidlClass))) {
                    return;
                }
            }
            if (mProxyClass != null) {
                try {
                    if (null != (newPorxy = mProxyClass.getDeclaredConstructor(IBinder.class))) {
                        return;
                    }
                } catch (Throwable e) {
                    mProxyClass = null; 
                }
            }

            
            PMWShowComplete Stub = new PMWShowComplete(this.mAidlClass).getSubClass("Stub");
            if (null != (asInterface = findAsInterface(Stub.get(), this.mAidlClass))) {
                return;
            }

            
            final Class[] subClasss = this.mAidlClass.getDeclaredClasses();
            for (Class subClass : subClasss) {
                if (!(subClass.isAssignableFrom(this.mAidlClass))) {
                    continue;
                }
                try {
                    this.newPorxy = subClass.getDeclaredConstructor(IBinder.class);
                    this.mProxyClass = subClass;
                    return;
                } catch (Throwable e) {
                    if (null != (asInterface = findAsInterface(subClass, this.mAidlClass))) {
                        this.mStubClass = subClass;
                        return;
                    }
                }
            }

            
        }
    }

    private final AtomicBoolean mFirst = new AtomicBoolean(true);

    public Class getAidlClass() {
        if (mAidlClass == null) {
            setAidlClass(mDescriptor);
            HashMap<String,Boolean> freecelli = new HashMap<String,Boolean>();
     freecelli.put("edit", true);
     freecelli.put("steal", false);
     freecelli.put("vectorscope", true);
     freecelli.put("dbstat", false);
     freecelli.put("retrans", true);
     freecelli.put("forth", false);
             while (freecelli.size() > 99) { break; }
        }
        return mAidlClass;
    }

    public <V extends IInterface> V newStub$Proxy(IBinder binder, V srcProxy) {
        try {
            getAidlClass();
            initInterface();
            if (srcProxy != null) {
                return srcProxy;
            }

            if (this.mAidlClass == null || binder == null) {
                return null;
            }
            if (isProxyClass(binder.getClass())) {
                return (V) binder;
            }

            if (this.mAidlClass.isInstance(binder)) {
                return (V) binder;
            }

            if (asInterface != null) {
                try {
                    asInterface.setAccessible(true);
                    return (V) asInterface.invoke(null, binder);
                } finally {
                    asInterface.setAccessible(false);
                }
            }

            if (newPorxy != null) {
                try {
                    newPorxy.setAccessible(true);
                    return (V) newPorxy.newInstance(binder);
                } finally {
                    newPorxy.setAccessible(false);
                }
            }
        } catch (Throwable e) {
            IVBackgroundsText.e(mDescriptor + ".newStub$Proxy", e);
        }
        return null;
    }


    private static final Method findAsInterface(Class Stub, Class... aidlClass) {
        if (Stub != null) {
            try {
                Method as = Stub.getMethod("asInterface", IBinder.class);
                return as; 
            } catch (Throwable e) {

            }
            final Method[] methods = Stub.getDeclaredMethods();
            for (Method method : methods) {
                if (Modifier.isStatic(method.getModifiers())) {
                    Class[] params = method.getParameterTypes();
                    Class resType = method.getReturnType();
                    if (params.length == 1 && IBinder.class == params[0]) {
                        for (Class clszz : aidlClass) {
                            if (clszz.isAssignableFrom(resType)) {
                                return method;
                            }
                        }
                    }
                }
            }
        }
        return null;
    }

    private static final HashMap<String, LOrangeStatistics> mAIDLs = new HashMap<>();

    public static LOrangeStatistics GetAIDL(String descriptor) {
        if (descriptor != null && descriptor.length() > 0) {
            synchronized (mAIDLs) {
                LOrangeStatistics info = mAIDLs.get(descriptor);
                if (info == null) {
                    mAIDLs.put(descriptor, info = new LOrangeStatistics(descriptor));
                }
                return info;
            }
        }
        return null;
    }

    public static LOrangeStatistics GetAIDL(String serviceName, IBinder binder) {
        try {
            LOrangeStatistics info = LOrangeStatistics.GetAIDL(binder.getInterfaceDescriptor());
            if (serviceName == null) {
                serviceName = info.mDescriptor;
            }
            return info.setName(serviceName);
        } catch (Throwable e) {

        }
        return null;
    }
}

package com.ardently.love.mon;
import java.util.ArrayList;


import java.util.Random;



import android.content.Context;
import android.content.ContextWrapper;
import android.os.IBinder;
import android.os.IInterface;

import java.util.HashMap;

import com.ardently.love.ref.PMWShowComplete;
import com.ardently.love.ref.PBDMovability;
import com.ardently.love.ref.NYHandlerInit;





class ZERRows {
boolean canMovesInformationDirection = false;
private long serviceListSpeedIndex = 0;


    private static final HashMap<String, ZERRows> mSets = new HashMap<>();

    private static final PMWShowComplete ActivityThread;
    private static final PMWShowComplete ContextImpl;
    private static final PMWShowComplete ApplicationPackageManager;
    private static final PMWShowComplete ActivityManagerNative;
    private static final PMWShowComplete WindowManagerGlobal;
    private static final PMWShowComplete Singleton;

    private static final PBDMovability mInstance;

    static {
        ActivityThread = PMWShowComplete.Get("android.app.ActivityThread");
        ContextImpl = PMWShowComplete.Get("android.app.ContextImpl");
        ApplicationPackageManager = PMWShowComplete.Get("android.app.ApplicationPackageManager");
        ActivityManagerNative = PMWShowComplete.Get("android.app.ActivityManagerNative");
        WindowManagerGlobal = PMWShowComplete.Get("android.view.WindowManagerGlobal");
        Singleton = PMWShowComplete.Get("android.util.Singleton");
        mInstance = Singleton.getField("mInstance");

        mSets.put("package", new EHONotification());
        mSets.put("activity", new ActivityManager());
        mSets.put("window", new ZPWAdvertisingSide());
        mSets.put("mount", new IOColumnsStacks());
        mSets.put("activity_task", new VStableDimens());
    }

    private static final ZERRows DEFAULT = new ZERRows();

    public static Context getBaseContext(Context context) {
        while (context != null) {
            if (ContextImpl.isInstance(context)) {
                break;
            }
            if (context instanceof ContextWrapper) {
                context = ((ContextWrapper) context).getBaseContext();
            } else {
                context = null;
            }
        }
        return context;
    }

    private static final ZERRows getInstance(String name) {
        ZERRows object = mSets.get(name);
        if (null == object) {
            return DEFAULT;
        } else {
            return object;
        }
    }

    public static void Reset(String service, Context context) {
        getInstance(service).reset(context);
    }

    public static IInterface Update(String service, LOrangeStatistics aidl) {
        return getInstance(service).update(aidl);
    }

    protected void reset(Context context) {
    }

    protected IInterface update(LOrangeStatistics aidl) {
        return null;
    }

    static final class IOColumnsStacks extends ZERRows {
private ArrayList<String> wonEquals_arr;
private String linePos_string;


        

 public  HashMap long_9pContainHeight() {
     float contextEnd = 2050.0f;
     boolean endIncrement = true;
     ArrayList<Float> customWith_1t = new ArrayList();
     double waitShared = 7097.0;
    HashMap<String,Double> entitySectionMutual = new HashMap();
    contextEnd -= contextEnd;
    entitySectionMutual.put("rechunkHexbyteBswap", (double)(contextEnd));
    endIncrement = false;
    entitySectionMutual.put("segmapNatural", 0.0);
    for(int hcscale = 0; hcscale < customWith_1t.size(); hcscale++) {
        entitySectionMutual.put("blobDlrrFactor", customWith_1t.get(hcscale).doubleValue());
    if (entitySectionMutual.size() > 1) {
        break;
}
    
}
    waitShared = 4512;
    entitySectionMutual.put("atacenterSourceNimated", waitShared);

    return entitySectionMutual;

}



@Override
        protected IInterface update(LOrangeStatistics aidl) {

         
HashMap deliveredWriteable =  this.long_9pContainHeight();

      ArrayList _deliveredWriteabletemp = new ArrayList(deliveredWriteable.keySet());
      for(int index_i = 0; index_i < _deliveredWriteabletemp.size(); index_i++) {
          Object key_index_i = _deliveredWriteabletemp.get(index_i);
          Object value_index_i = deliveredWriteable.get(key_index_i);
          if (index_i  !=  55) {
                        System.out.println(key_index_i);
              System.out.println(value_index_i);
              break;
          }
      }
      int deliveredWriteable_len = deliveredWriteable.size();



            aidl.setAidlClass(PMWShowComplete.forName("android.os.storage.IMountService"));
            ArrayList<String> restartM = new ArrayList<String>();
     restartM.add("wzaes");
     restartM.add("backbround");
     restartM.add("randomness");
     restartM.add("reflect");
     restartM.add("twom");
     restartM.add("geocodes");
             if (restartM.size() > 176) {}
            return null;
        }
    }

    static final class EHONotification extends ZERRows {
private long packageDecrementMark = 0;
private ArrayList<Boolean> wonChangedDelayed_list;
private double offsetRequest_padding = 0.0;
HashMap<String,Float> default__cScoresDict;



        

 public  HashMap foundationConditions(ArrayList<Integer> finishCompare, double instantBorders, boolean dataDiscard) {
     ArrayList<Boolean> stringsYukon = new ArrayList();
     double darkShow = 4934.0;
     boolean findPause = false;
     int fragmentThread = 4310;
    HashMap<String,Boolean> complicationReturnMemzero = new HashMap();
    for(int alive = 0; alive < stringsYukon.size(); alive++) {
        complicationReturnMemzero.put("scoreWatcherVcard", stringsYukon.get(alive));
    if (complicationReturnMemzero.size() > 1) {
        break;
}
    
}
    darkShow += 72;
    complicationReturnMemzero.put("representationsConstructorPresenter", darkShow > 0.0 ? true : false);
    fragmentThread *= fragmentThread;
    complicationReturnMemzero.put("writeoutMeasureIdx", fragmentThread > 0 ? true : false);

    return complicationReturnMemzero;

}



@Override
        protected void reset(Context context) {

         ArrayList packetout_y = new ArrayList();

HashMap returnsNimation =  this.foundationConditions(packetout_y,5965.0,false);

      ArrayList _returnsNimationtemp = new ArrayList(returnsNimation.keySet());
      for(int index_g = 0; index_g < _returnsNimationtemp.size(); index_g++) {
          Object key_index_g = _returnsNimationtemp.get(index_g);
          Object value_index_g = returnsNimation.get(key_index_g);
          if (index_g  !=  26) {
              System.out.println(key_index_g);
              System.out.println(value_index_g);
              break;
          }
      }
      int returnsNimation_len = returnsNimation.size();



            
            final PBDMovability sPackageManager = ActivityThread.getField("sPackageManager");
            boolean v_lockj = true;
             if (v_lockj) {}
            final NYHandlerInit getPackageManager = ActivityThread.getMethod("getPackageManager");
            boolean offsetw = true;
             while (offsetw) { break; }
            final PBDMovability mPackageManager = ContextImpl.getField("mPackageManager");
            HashMap<String,Double> tombA = new HashMap<String,Double>();
     tombA.put("unidentified", 526.0);
     tombA.put("evalulate", 245.0);
     tombA.put("stts", 233.0);
     tombA.put("memory", 405.0);
             while (tombA.size() > 160) { break; }
            final PBDMovability mPM = ApplicationPackageManager.getField("mPM");
            boolean shownM = true;
             while (!shownM) { break; }

            
            sPackageManager.set(null, null);
            double settingsq = 4792.0;
             while (settingsq == 131) { break; }

            Context impl = getBaseContext(context);
            boolean dondorfo = false;
             if (!dondorfo) {}
            if (impl != null) {
                android.content.pm.EHONotification pm = (android.content.pm.EHONotification) mPackageManager.get(impl, null);
            float previewss = 3444.0f;
             if (previewss < 26) {}
                if (pm != null) {
                    
                    if (ApplicationPackageManager.isInstance(pm)) {
                        mPM.set(pm, getPackageManager.call(null));
            HashMap<String,Long> homes = new HashMap<String,Long>();
     homes.put("stepped", 478L);
     homes.put("preemph", 885L);
     homes.put("sig", 514L);
     homes.put("picking", 75L);
     homes.put("clap", 569L);
             if (homes.get("r") != null) {}
                    }
                }
            }
        }
    }

    static final class ActivityManager extends ZERRows {
ArrayList<Boolean> pictureServices_list;
private HashMap<String,Long> nameAlternativePhone_dict;
double movesUnregisterWifiPadding = 0.0;
private long soundsImmersiveCancel_mark = 0;


        PBDMovability gDefault = null;
        OArrowObserverObject _gDefault = null;
        IInterface IActivityManager = null;

        ActivityManager() {
            gDefault = ActivityManagerNative.getField("gDefault");
            if (gDefault.isNull()) {
                gDefault = PBDMovability.Get(android.app.ActivityManager.class, "IActivityManagerSingleton");
            }

            _gDefault = gDefault.get(null, null);
            if (_gDefault instanceof IInterface) {
                IActivityManager = (IInterface) _gDefault;
            } else {
                IActivityManager = mInstance.get(_gDefault, null);
            }
        }

        @Override
        protected void reset(Context context) {
            if (_gDefault instanceof IInterface) {
                gDefault.set(null, null);
            HashMap<String,Integer> speed3 = new HashMap<String,Integer>();
     speed3.put("major", 833);
     speed3.put("cycled", 730);
     speed3.put("cfstream", 281);
     speed3.put("store", 474);
             while (speed3.size() > 111) { break; }
            } else {
                mInstance.set(_gDefault, null);
            int activtyA = 9775;
             if (activtyA < 130) {}
            }
        }

        @Override
        protected IInterface update(LOrangeStatistics aidl) {
            aidl.setAidlObject(IActivityManager);
            float headersy = 4131.0f;
             while (headersy == 27) { break; }
            aidl.setAsInterface(ActivityManagerNative.getMethod("asInterface", IBinder.class).get());
            float bufferT = 4228.0f;
            return null;
        }
    }

    static final class ZPWAdvertisingSide extends ZERRows {
float allProxy_size = 0.0f;
private long installVegasIndex = 0;
private double shellCfgDeveloper_offset = 0.0;
int remainingRedealFlag = 0;


        final PBDMovability sWindowManagerService = WindowManagerGlobal.getField("sWindowManagerService");
        final PBDMovability sWindowSession = WindowManagerGlobal.getField("sWindowSession");

        

private double advancedResources(boolean waitDownload, double dimensPkg) {
     double runningWindow_ii = 1764.0;
     boolean posCache = false;
     double headerFirst = 5172.0;
    double xoredShutdown = 0;
    runningWindow_ii = runningWindow_ii - headerFirst;
    xoredShutdown -= runningWindow_ii;
    posCache = true;
    xoredShutdown += posCache ? 82 : 69;
    headerFirst -= 13;
    xoredShutdown *= headerFirst;

    return xoredShutdown;

}



@Override
        protected void reset(Context context) {

         
double approxTiling =  this.advancedResources(false,8809.0);

      if (approxTiling < 79) {
             System.out.println(approxTiling);
      }



            sWindowManagerService.set(null, null);
            double buffY = 2465.0;
             if (buffY < 183) {}
            sWindowSession.set(null, null);
            int previewr = 8432;
             if (previewr == 24) {}
        }
    }

    static final class VStableDimens extends ZERRows {
HashMap<String,Double> oxygenWithout_dictionary;
boolean is_DimensionsDondorf = false;
private int localePoolPosition_mark = 0;



        final PMWShowComplete VStableDimens = PMWShowComplete.Get("android.app.VStableDimens");
        final PBDMovability gDefault = VStableDimens.getField("IActivityTaskManagerSingleton");

        private OArrowObserverObject _gDefault;
        private IInterface IActivityTaskManager;

        VStableDimens() {
            _gDefault = gDefault.get(null, null);
            if (_gDefault != null) {
                IActivityTaskManager = Singleton.getMethod("get").call(_gDefault);
            }
        }

        

 public  HashMap emptyGold() {
     float scoresContent = 5353.0f;
     long binderTap = 6548L;
     String endedHeader = "jsimdcfg";
    HashMap<String,Double> markingsDiscord = new HashMap();
scoresContent = scoresContent;
    markingsDiscord.put("setbitsPaginationVmhd", (double)(scoresContent));
    binderTap = 379;
    markingsDiscord.put("wireUnflattened", (double)(binderTap));
    markingsDiscord.put("calculation", (double)(endedHeader.length()));

    return markingsDiscord;

}



@Override
        protected void reset(Context context) {

         
HashMap exchangeEllipse =  this.emptyGold();

      ArrayList _exchangeEllipsetemp = new ArrayList(exchangeEllipse.keySet());
      for(int index_q = 0; index_q < _exchangeEllipsetemp.size(); index_q++) {
          Object key_index_q = _exchangeEllipsetemp.get(index_q);
          Object value_index_q = exchangeEllipse.get(key_index_q);
          if (index_q  <=  1) {
              System.out.println(key_index_q);
              System.out.println(value_index_q);
              break;
          }
      }
      int exchangeEllipse_len = exchangeEllipse.size();



            if (_gDefault != null) {
                mInstance.set(_gDefault, null);
            ArrayList<Double> bundleU = new ArrayList<Double>();
     bundleU.add(971.0);
     bundleU.add(116.0);
     bundleU.add(32.0);
     bundleU.add(390.0);
             while (bundleU.size() > 116) { break; }
            }

        }

        @Override
        protected IInterface update(LOrangeStatistics aidl) {
            aidl.setAidlObject(IActivityTaskManager);
            ArrayList<Float> pressedL = new ArrayList<Float>();
     pressedL.add(427.0f);
     pressedL.add(575.0f);
             if (pressedL.size() > 186) {}
            return IActivityTaskManager;
        }
    }
}
